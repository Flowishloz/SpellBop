# WIZARD DODGEBALL — HANDOFF.md
> Master Context File. Immutable source of truth alongside `Wizard_Dodgeball_Manifesto.txt`,
> `Wizard_Dodgeball_Graphics_Pipeline.txt`, `Cards_Spells.txt`, and `CARD_FRAMEWORK.md`.
> The Orchestrator updates the Graveyards below whenever a mechanic or bug is formally rejected/resolved.

## TEAM PROTOCOL
- **Orchestrator (Lead Agent):** Delegation, architecture review, Human Integration Tasklists. Closing Ceremony documentation (Project_State.txt / HANDOFF.md sync) is delegated to Agent Delta after code review. Autonomy Protocol (2026-06-11): Orchestrator has full autonomous authority over all code, scene, and resource files; the human is Creative Director + playtester and is no longer asked to perform editor wiring.
- **Agent Alpha:** Deterministic physics (SG Physics 2D only — NO float gameplay math), rollback input buffers.
- **Agent Beta:** Nakama backend (127.0.0.1:7350, console :7351), auth, matchmaking, card inventory RPCs.
- **Agent Gamma:** StateCharts (100%/10% engine speed), Custom Resources, decoupled Components, signal-driven UI.
- **Agent Delta (The Archivist):** Documentation, state management, context preservation. Exclusively owns `Wizard_Dodgeball_Brain/`; never writes engine code (.gd/.tres/scenes).

## ENGINE STACK
- Godot 4.6 | Forward Plus | D3D12 | Jolt (3D only — gameplay physics is SG Physics 2D)
- Hybrid 2.5D: invisible SG2D fixed-point sim (X/Y) → visible 3D projection (X/Z)
- Time dilation: TheStack autoload (core/the_stack.gd) — Engine.time_scale 10% Stack window (2.5 s default), wall-clock timed (rollback-safe: re-paces tick presentation, never tick math); the window IS the staging countdown: spell_staged opens/RESETS it (counter slaps included) and MatchController resolves the stack LIFO via release_staged() on stack_closed — one event, one clock; window_fraction_remaining() feeds the Counter Window-of-Affect
- Target platform: iOS/Android portrait 9:16 (1080x1920, canvas_items stretch), desktop test window 486x864; Android arm64 APK builds (Mobile renderer on device, Forward+ on desktop); Android toolchain at D:\Games\GODOT\android-tools\
- Netcode: Bimdav Delta Rollback — **ENABLED for desktop (Sprint 21)** as the LAN + Nakama-online rollback layer (`SyncManager` autoload). Every gameplay object implements its `_network_process`/`_save_state`/`_load_state` contract. **delta_rollback cross-compiled for android arm64-v8a (debug+release) in `addons/delta_rollback/bin/`** alongside the desktop libs (Windows/Linux/macOS); the `android.debug.arm64` / `android.release.arm64` lines are RESTORED in the `.gdextension` (mobile-MP enablement A, commit `151427d`) — the on-device extension LOAD is the Creative Director's playtest. sg-physics-2d cross-compiled for android arm64 (debug+release) in addons/sg-physics-2d/lib/
- Plugins: **Godot StateCharts** drives the multiplayer menu (Sprint 21 — used at runtime via `class_name`, no editor-plugin enablement needed); **delta_rollback** GDExtension enabled for desktop. Phantom Camera / Retro-PS2 shader stack on disk under `addons/`.
- **Gameplay Phase (Sprint 18, 2026-06-14):** Cards commit on the press edge (no channel time). Base fireball keeps its 3-gauge charge with gauge-colour button ring (yellow/red/blue thirds), escalating screen+sprite shake per gauge, and size scaling 1.0/1.10/1.25 per gauge level.
- **Phase 1 / Sprint 19 (2026-06-14):** Major gameplay overhaul. (1) The stack now resolves LIFO ONE SPELL AT A TIME in slow-mo with a 0.2 s real-time gap between each release; normal speed resumes only after the FINAL spell (the_stack.gd: close_window() = instant KO snap-back, _expire_window() = hold dilation for staggered resolution, resume_speed(); MatchController._resolve_stack_entry recurses via an ignore_time_scale SceneTreeTimer). (2) STACK WINNER = the last responder (whoever placed the NEWEST spell, always, even uncontested) banks a 1.5x speed boost on their NEXT fired projectile (PlayerController.grant/consume_speed_boost, saved as the "psb" sim-state key; consumed in SpellCasterComponent._spawn_projectile and CardCasterComponent._resolve_attack; MatchController.stack_winner_decided(player_won) signal is the Phase-3 indicator hook). (3) A fully-charged (gauge 3) base fireball SHATTERS the Icey Retort frost wave and powers through it, reusing the broken-defense glass burst (FireballController.shatters_ice armed by SpellCaster when fired charge_level>=3; _scan_ice_shatter + get_collider_half_extents + shatter_ice). (4) The opponent AI no longer mirrors the player's X: AIBrainComponent._decide_x now steers to an independent target — a deterministic triangle-wave patrol (_patrol_target_x) plus emerald-seek-while-hurt — with the dodge logic unchanged. (5) Projectile lifespans DOUBLED (fireball.tscn 2.2->4.4 s, ice_wave.tscn 1.5->3.0 s, basic_attack.tres Spark Bolt lifetime 2.8->5.6 s, spark_bolt.tscn 2.2->4.4 s). (6) HEALING EMERALD: scripts/pickups/emerald_controller.gd (EmeraldController, SGCharacterBody2D) + scenes/emerald.tscn — a glowing green gem spawns near arena centre every 25-40 s (deterministic LCG cadence in MatchController._physics_process, gated to ROUND_ACTIVE), FLOATS + SPINS (visual only) and DRIFTS randomly (deterministic integer LCG, fixed-point, reflecting inside a wander box). Striking it with a projectile grants the THROWER +1 life (HealthComponent.apply_heal) and consumes both. It joins the "pickups" group and deliberately exposes NO get_velocity_y/get_hit_source/slow_ticks/redirect, so every existing sim scan (AI dodge, defense WOA, barrier capture, ice-shatter) skips it for free. (7) The Left-Shift DASH ability was REMOVED WHOLESALE (movement_component rewritten without dash, InputCommand KEY_DASH gone, PlayerController latch gone, the DashButton HUD node + dash_button_hud.gd deleted, the "dash" InputMap action removed from project.godot, the dash SFX trigger removed, visual_bridge_component's dash smoothing-boost removed). All 13 headless test suites pass and determinism stays BIT-IDENTICAL; 3 new suites added (tests/test_stack_resolution.gd, tests/test_ice_shatter.gd, tests/test_emerald.gd). The project is now under git version control, backed up to https://github.com/Flowishloz/SpellBop.git (branch main).
- **Phase 2 / Sprint 19 — Camera (2026-06-14):** All in scripts/visual/camera_rig.gd. (1) Default perspective pushed BACK 15% (pullback_factor scales the camera's height + depth in _ready, keeping the lateral shoulder offset) and the default FOV widened 15% (base_fov_scale) — the script now OWNS fov (.tscn keeps the authored 71). (2) Smoother tracking that lingers centre longer: follow_strength 0.85->0.55, follow_speed 8->5.5, shoulder_swap_threshold 0.7->1.15, shoulder_glide_speed 2.4->1.5, plus a max_yaw_degrees hard clamp guarding against jarring flips past a sane angle. (3) DYNAMIC STACK ZOOM: while the world is dilated (the stack resolving in slow-mo, detected via Engine.time_scale < stack_zoom_time_threshold) the camera tightens its FOV to base_fov * stack_zoom_scale (0.85 = zoom in 15%), easing back as normal speed resumes. Verified by tests/test_camera_zoom.gd (FOV 81.7 default -> 69.8 in-stack -> 81.6 resumed) and a screenshot; determinism unaffected (camera is presentation-only).
- **Phase 3 / Sprint 19 — UI (2026-06-14):** (1) The fireball cast button moved to the bottom-right corner and enlarged 70% (radius 84 -> 142.8) in scenes/ui/match_hud.tscn. (2) Card hand reordered via a VISUAL_ORDER remap in scripts/ui/card_hand_hud.gd: DEFENSE is anchored at the BOTTOM of the fan, COUNTER takes the middle — the card-caster SLOTS (keys 8/9/0) are UNCHANGED, only the on-screen order swaps (used in _dock_pos/_dock_rot/z_index/gradient/_card_hit). (3) Hand-casting feedback in scripts/ui/stack_display_hud.gd: every staged attack/counter card now GLOWS its type colour (red/blue bloom, components pushed above 1.0) and shakes, intensifying with franticness as the countdown runs down. (4) Stack winner indicator: a bold "YOU/FOE WIN THE STACK!" banner pops + fades the instant the stack resolves, driven by the MatchController.stack_winner_decided(player_won) signal added in Phase 1. (5) Round-start visual in scripts/ui/match_flow_overlay.gd: a dedicated BOLD popped "ROUND N" label (font 104, kept off _title so result screens keep their size) framed by two procedurally-drawn horizontal lightning bolts — the TOP bolt shoots right, the BOTTOM shoots left (inner class _RoundLightning, animated on the wall clock). All 14 headless suites pass; five screenshots confirm each change.
- **Phase 4 / Sprint 19 — Graphics (2026-06-14):** (1) The fully-charged (gauge 3) fireball already renders 35% larger than base — CHARGE_SIZE_MULTIPLIERS = [1.0, 1.0, 1.15, 1.35] in spell_caster_component.gd, confirmed by test_charge_mechanics (32.4u). (2) 80s main-menu backdrop in scripts/ui/home_screen.gd (_build_backdrop): a script-built synthwave GradientTexture2D sunset sky (purple->magenta->orange, unshaded), a big ringed planet (emissive sphere + tilted neon TorusMesh), a small second planet, and a star field. (3) Glowing arena borders in core/match_controller.gd (_build_arena_borders, called from _ready): four emissive cyan BoxMesh strips framing the 9.6x21 floor perimeter. (4) DEATH ANIMATION in scripts/visual/wizard_animator_component.gd: on a LETHAL hit (HealthComponent.knocked_out) the wizard is flung BACKWARD off its own baseline edge — a visual physics gravity arc on the SPRITE's local offset (the VisualBridge owns rig X/Z, so the fling can't fight it), with a flat-spin (scale.x flip, since the Y-billboard ignores node rotation), a red fade, and an impact burst; reset on the next round when health refills (health_changed). All 14 suites pass; screenshots confirm the backdrop, borders, and fling.
- **Phase 5 / Sprint 19 — Game Feel (2026-06-14):** (1) Smoother slow-mo -> normal transition: the_stack.resume_ramp_rate raised 7 -> 11 for a snappier lag-free speed-up, and MatchController resolves the FINAL staged spell then resumes after only a short post_resolve_delay_seconds (0.12 s) beat instead of the full 0.2 s inter-spell stagger, so the dilation never lingers. (2) Firing impact amplified 20%: a fire_shake_multiplier (1.2) multiplies the camera trauma at every projectile spawn in MatchController._on_spell_cast. Verified by tests/test_fire_shake.gd (peak trauma 0.180 = base 0.15 x 1.2) and the full 15-suite sweep (determinism bit-identical). This COMPLETES the Sprint 19 five-phase gameplay/UI/graphics overhaul; the whole project is version-controlled and pushed to the GitHub backup at https://github.com/Flowishloz/SpellBop.git (branch main).
- **Sprint 20 / Round-2 Overhaul (2026-06-14):** UI polish, freeze reliability, staggered-resolution visibility, healing emerald cap, wall placement, and KNOCKOUT SEQUENCE cinematic. (1) Post-round banner text: "ROUND WON" / "ROUND LOST" (match_flow_overlay.gd). Fanned card hand pulled 20% further into screen (card_hand_hud.gd DOCK_X 1102/1074/1040 -> 1054/1026/992). Desktop keycodes swapped (project.godot keys 8/9/0 ATTACK/COUNTER/DEFENSE top->middle->bottom, touch/AI unchanged). (2) HitDetectionComponent widens overlap band by projectile per-tick velocity (anti-tunnel), so fast 2x-reflected Icey Retort can't step PAST owner between ticks. (3) Stack resolution VISIBLE: the_stack.gd `_expire_window()` steps dilation to moderate `resolve_time_scale` (0.3), and MatchController `stagger_delay_seconds` 0.2 -> 0.4; each spell now visibly travels (test gap 393 ms, was near-simultaneous). (4) Healing emerald capped `emerald_max_per_match` (2) per MATCH (exactly one on field at a time); placeholder sphere replaced with faceted Chaos-Emerald brilliant-cut mesh (SurfaceTool, emissive green); slower spin (1.0), subtler hover (0.10), wider wander box (±440/±560) bumping walls. (5) Side-wall hitboxes moved OUT so inner face sits at visible glow perimeter (match_arena.tscn fixed_position_x ±30801920 -> ±32768000, centre ±500 / inner ±480 vs glow ±475). Wall pulses spawn ON wall face (ball centre pushed out by collider half-extent) instead of at projectile centre; frost wave opts OUT of wall pulses (FireballController.emits_wall_pulse false). (6) KNOCKOUT SEQUENCE (the big one): cinematic death beat BEFORE result overlay. Slow-mo (the_stack.gd `hold_dilation`, death_time_scale 0.3). Death cam zooms FOV in (camera_rig.gd, death_zoom_scale 0.5) onto eliminated wizard, tracking fling via look_at. Death fling runs on SCALED delta (wizard_animator_component `_update_death`). Bigger explosion (BurstFX ≈80 fiery + 46 white particles). Screen-space ripple shockwave from contact (retro_lens.gdshader ripple uniforms, MatchController `_update_death_ripple`). Floor shadow HIDES on KO, returns on revive (DropShadowComponent). Post-round overlay WAITS `death_sequence_seconds` (2.5 s) while animation plays (MatchController `_begin_death_sequence` / `_finish_death_sequence` via wall-clock SceneTreeTimer; `_in_death_sequence` guard parks round-flow). Bold VICTORY (yellow) / DEFEAT (red) verdict heading pops HIGH on screen, framed by horizontal lightning, holds through beat (match_flow_overlay `_on_knockout_began` + `_round_hold` mode). New signal: `MatchController.knockout_began(is_match_end: bool, player_won: bool)`. All 14 headless suites pass, determinism BIT-IDENTICAL. New test suites: tests/test_death_sequence.gd, tests/test_emerald_cap.gd. Existing suites updated to collapse death beat (death_sequence_seconds small + death_time_scale 1.0) + wait for delayed round/match signals. Screenshots confirm chaos-emerald gem + death sequence. Project backed up to https://github.com/Flowishloz/SpellBop.git (branch main).
- **Sprint 20 follow-up (2026-06-14):** Creative Director re-report + fix: (1) STACK still clusters — the 0.2/0.4 s gap staggered TIMING but was too quick to read, and the final spell snapped back to normal speed immediately so spells still felt simultaneous. FIX: MatchController.stagger_delay_seconds 0.4 -> 0.85 (a clearly readable ~0.85 s pause between each LIFO release — timer ignore_time_scale so gap is real regardless of dilation), post_resolve_delay_seconds 0.12 -> 0.55 (the LAST released spell now gets slow-mo travel time before normal speed resumes, not an instant launch-then-snap), and the_stack.resolve_time_scale 0.3 -> 0.35. Result: first released spell is ~1369 sim units down-court (~77% of the ~1760-unit court) by second launch — unmistakably one-at-a-time. tests/test_stack_resolution.gd now asserts SIM-POSITION separation (>200 units), not just wall-clock gap. (2) KNOCKOUT ZOOM by outcome: death cam's flat 50% zoom is now outcome-based — TIGHTER zoom celebrating a WIN (death_zoom_scale_win = 0.65, 35% zoom-in) and GENTLER on a LOSS (death_zoom_scale_lose = 0.80, 20% zoom-in). New MatchController exports; camera_rig.begin_death_cam(sprite, zoom_scale) takes per-knockout FOV factor; MatchController._begin_death_sequence picks it from player_won_round. All 14 headless suites still pass, determinism BIT-IDENTICAL; win/loss death-cam framings confirmed by screenshots.
- **Sprint 20 round-3 (2026-06-14):** TWO root-cause fixes for re-reported Creative Director issues. (1) STACK still resolved staggered but the on-screen DISPLAY cleared at once — the projectiles' LIFO release was mechanically correct, but the MTG-style stack-display card pile (scripts/ui/stack_display_hud.gd) flew ALL card faces away simultaneously when the window expired (_on_stack_closed), so the player watched the cards vanish together even though the projectiles were staggered. FIX: peel the display cards ONE AT A TIME off each spell's card_cast signal (which fires once per LIFO release, ~0.85 s apart), connecting via _on_card_resolved + _make_leave; the display only clears the whole pile on a KO or forced close (when staged spells never resolve), not on normal expiry. DEFENSE card_casts are ignored (instant, never staged). Verified: test_stack_resolution asserts exactly ONE card is leaving on the first resolution (leaving=1, staying=1), not all at once. (2) REFLECTED ICE WALL sometimes doesn't freeze the recipient — the anti-tunnel band made the straight reflected wave connect reliably, but the defense barrier's random lateral ricochet (position-parity hash) sent the court-wide frost wall off-angle, so it intermittently missed its thrower. FIX: a reflected FROST wave (slow_ticks > 0) flies STRAIGHT (vx = 0, no ricochet) AND gently HOMES onto its ORIGINAL thrower (captured from get_hit_source() before the hit source is overwritten to the defender; set_homing(thrower, 0.4) in scripts/arena/barrier_controller.gd _tick_capture), so a wide ice wall reliably returns even if the thrower stepped aside. Fireballs keep the ricochet; only slow_ticks>0 waves reflect straight+homing. Verified by tests/test_reflect_freeze.gd (anti-tunnel freeze works; barrier-reflected ice homes onto a thrower at +330 units offset and freezes). All 18 headless suites pass, determinism BIT-IDENTICAL. New suite: tests/test_reflect_freeze.gd.
- **Sprint 20 round-4 (2026-06-14):** Creative Director playtest follow-ups: STACK RESOLUTION REVERTED to SIMULTANEOUS (the staggered one-at-a-time playtest felt wrong; "I want to see all spells resolve AT ONCE"). MatchController._on_stack_closed now releases ALL staged spells in one frame (LIFO order only for same-frame counter-before-attack), then resume_speed(); the staggered _resolve_stack_entry recursion and the stagger_delay_seconds / post_resolve_delay_seconds exports were removed; the_stack.resolve_time_scale was removed and _expire_window no longer steps the dilation; stack_display_hud._on_stack_closed flies the WHOLE card pile away together again (the per-card card_cast peel was removed). test_stack_resolution rewritten to assert SIMULTANEOUS resolution (both projectiles spawn <100 ms apart, the display clears all cards together). (2) SHIELD REFLECT DRAMA: a barrier CATCHING a fireball is now a slow-mo anticipation beat so the Window-of-Affect reflect is always visible ("sometimes I don't see it"). On capture_started MatchController drops the world to shield_capture_time_scale (0.4) for the hold and resumes on release (guarded against the death beat / an open stack window); BarrierController gained min_capture_ticks (24) so even a low-WOA early block holds visibly; basic_defense.tres woa_max_hold_seconds raised 0.6 -> 1.1 (longer charge for a last-second block); the capture rumble was hardened (0.3 -> 0.9 across the hold) and the release slam raised (capture_release_trauma 0.55 -> 0.9). (3) SCREEN RIPPLE reused: the knockout's retro-lens displacement shockwave (MatchController._trigger_ripple, generalized to take a world point + strength) now ALSO fires when a shield catches a fireball (at the wall, shield_ripple_strength) and when the emerald is struck (at the gem, emerald_ripple_strength). The emerald emits a new claimed(world_pos) signal that MatchController connects at spawn. (4) HEART-POP EMERALD CUE: new scripts/visual/heart_pop_fx.gd (class_name HeartPopFX) — when the emerald is struck a red HEART (two emissive lobes over a downward cone, shared static mesh/material) pops vertically out of the gem, arcs up, falls under gravity, and DESPAWNS the instant it touches the floor — a clear "you gained a life" visual cue (Creative Director). Spawned from EmeraldController._grant_heal alongside the green heal burst. (5) KNOCKOUT CAMERA: (a) the VICTORY/DEFEAT verdict banner is now emitted in the SAME frame the death cam begins, so the banner and the zoom start together. (b) The abrupt FOV-cut zoom was replaced by a smooth DOLLY ZOOM (camera_rig.gd): the camera glides from its CURRENT framing physically IN to a close shot over dolly_seconds (1.5 s, smoothstep ease) while the FOV WIDENS toward dolly_fov_widen (1.15) — a sports-broadcast vertigo push-in that tracks the sprite via look_at (no cut). The death_zoom_scale_win/lose exports were repurposed as the dolly END-distance TIGHTNESS (win 0.65 = a closer/tighter shot, loss 0.80 = gentler). All 18 headless suites pass, determinism BIT-IDENTICAL; screenshots confirm the win/loss dolly-zoom framings and the heart pop.
- **Sprint 21 / Multiplayer Framework (2026-06-14):** Dual-protocol netcode (LAN + Nakama online) standing up the menu→match path, both funnelling into the **delta_rollback `SyncManager`**. On branch `feature/multiplayer-netcode`. **ARCHITECTURE (anti-spaghetti spine):** a single `NetworkManager` autoload (`core/network_manager.gd`) is the only thing the UI talks to; two transport modules behind an identical tiny contract each do ONE structural thing — assign `multiplayer.multiplayer_peer` — so everything downstream is transport-blind: `core/net/lan_lobby.gd` (`PacketPeerUDP` beacon discovery on :24546 incl. a 127.0.0.1 loopback for same-machine testing + `ENetMultiplayerPeer` host/join on :24545) and `core/net/nakama_lobby.gd` (Nakama device-auth at 127.0.0.1:7350 / `defaultkey`, friends search-by-id/add/accept, private-match **invite via a direct-message channel** carrying the match id, `NakamaMultiplayerBridge`). **UNIFIED HANDSHAKE:** from the moment a `MultiplayerPeer` is live the path is byte-identical — `NetworkManager` owns `multiplayer.peer_connected`, both peers `change_scene_to_file(match_arena)` with `netplay=true`, `MatchController._ready` calls `NetworkManager.notify_scene_loaded()`, a reliable `@rpc` ready-exchange barriers both peers, then `core/net/rollback_session.gd` registers the peer and (host only) calls `SyncManager.start()` (which RPCs `_remote_start`, waits half-RTT, fires `sync_started` on both). **MENU:** a real `godot_state_charts` StateChart built IN CODE (`scripts/ui/menu_flow.gd` + `scenes/ui/menu_flow.tscn`): Main → QuickMatch → {Offline (today's Story-Mode sandbox launch, no net) / Local (Host/Search) / Online (Connect/Friends/Invite)} → Handshake, one panel per state. Story Mode lives on as a RESERVED (disabled) campaign portal (`scripts/ui/home_screen.gd` rewired to mount the flow). **ROLLBACK WIRING (two WIZARDS this sprint):** `MatchController._enter_netplay()` removes the opponent AIBrain, assigns deterministic ownership (HOST owns Player, CLIENT owns Opponent — both peers compute the same pair), adds both wizards to the `"network_sync"` group, stops the local tick drivers (`_set_sim_running` no-ops in netplay) and disables the (not-yet-rollback-routed) emerald spawner; `PlayerController._get_local_input()` gates by `is_multiplayer_authority()` (owner returns real input, the other returns `{}` and the net supplies it). Casting/cards are SUPPRESSED in netplay (`netplay_casting_enabled=false`) until spawns go through `SpawnManager` (next sprint). **CONFIG:** `delta_rollback` GDExtension re-enabled for desktop (android.* trimmed); `SyncManager`+`NetworkManager` autoloads added; two Godot-4 API-rename compat shims in the addon. **VERIFIED:** all **19** headless suites pass (determinism BIT-IDENTICAL; new `tests/test_net_handshake.gd` drives the StateChart), and `tests/run_net_smoke.ps1` launches TWO local instances that connect over ENet, complete the handshake, reach `sync_started`, and at sim-tick 120 print **byte-identical wizard fingerprints** (both wizards driven by synced input to the clamped edge x=400). NOT re-exported to Android this sprint. **NEXT:** route every projectile/barrier/emerald/heart spawn through `SpawnManager` (makes casting rollback-correct), cross-compile `delta_rollback` arm64 for mobile, and point the client camera at its own wizard.

- **Sprint 22 / Netplay client charge-rumble fix (2026-06-15):** Playtest: player 1 (HOST, blue) charging a fireball shook player 2 (CLIENT, red)'s screen, as if the client were charging. The local-player charge rumble + throw-stat feedback was wired to the blue Player in `MatchController._ready` and never retargeted on the client — the **4th** miss in the client-perspective retarget class (after the cast-button ring, the card-hand HUD, and the health bars). On branch `feature/spawn-rollback-cards`; commit `44e67b0`. FIX: extracted the wiring into `_wire_local_charge_feedback(wizard, connect_it)`; `_ready` defaults it to the blue Player (offline + the netplay HOST), and `_enter_netplay` retargets it to the red Opponent on the CLIENT (disconnect from Player, connect to Opponent) inside the existing `if not is_host:` block beside the camera + health-bar swaps. Presentation-only — 19 headless suites BIT-IDENTICAL; the netplay retarget is confirmed by the Creative Director's playtest (camera/UI client-perspective fixes are not headless-testable). **RULE (recurring class):** any presentation hardwired to `_player` (the blue Player) must be retargeted to the LOCAL wizard on the client in `_enter_netplay` — audit for it whenever touching local-player camera/HUD/feedback. Also: restored the project name to "Wizard Dodgeball Main" (Creative Director's rename; commit `6aadd85`).
- **Mobile Multiplayer Enablement — A + B + B2 (2026-06-15):** Items A, B, and B2 (B2a + B2b) of the MOBILE-MP NEXT PHASE are DONE on branch `feature/spawn-rollback-cards` (4 commits); only item **C** (the real-online Nakama + real-latency validation gate) remains. **A — delta_rollback cross-compiled for Android arm64 (commit `151427d`).** `delta_rollback` is a C++ godot-cpp/`SConstruct` GDExtension (`src/*.cpp`, root `SConstruct`→`godot-cpp/SConstruct`, NOT pure GDScript). Cross-compiled `libdelta_rollback` for android arm64-v8a, debug (723520 B) + release (664720 B), verified AArch64 ELF, via godot-cpp branch 4.2 (cloned into `godot-cpp/`; the `.gitmodules` gitlink was never committed) + NDK r25c. NDK trap: godot-cpp 4.2's android tool hard-pins NDK 23.2.8568313 (`$ANDROID_HOME/ndk/<ver>`) and KeyErrors on a missing `ANDROID_HOME` — worked around with a directory junction `ndk\23.2.8568313`→`25.2.9519653` + `ANDROID_HOME=<sdk>`. Dropped the two `.so` into `addons/delta_rollback/bin/` and RE-ADDED `android.debug.arm64` / `android.release.arm64` to `addons/delta_rollback/bin/delta_rollback.gdextension` (Sprint 21 had trimmed them). Fixed a LATENT Sprint-21 EXPORT bug: `export_presets.cfg` `exclude_filter` excluded `addons/delta_rollback/*` AND `addons/godot_state_charts/*` — both are HARD runtime deps now (the SyncManager autoload + the menu StateChart), so the APK would have crashed on launch; removed both from the filter (`phantom_camera` stays excluded — confirmed unused at runtime). Installed the matching 4.6.3 export templates (the box only had 4.6.1, which has the GDExtension regression). Re-exported `SpellBop.apk` (~50 MB, signed); verified `lib/arm64-v8a/` contains `libdelta_rollback...arm64.so` + `libsgphysics2d...arm64.so` plus the delta_rollback (83) + godot_state_charts (92) script assets, NO desktop-binary or godot-cpp bloat. New `.gitignore` entries: `/bin/`, `/godot-cpp/`, `src/*.os`, `tests/_*.log`/`.err` (build artifacts). The on-device extension LOAD is the Creative Director's playtest (no headless way to verify arm64 loading). **B — netplay touch-movement input fix (commit `1d5973b`).** THE BUG: the touch joystick presses the DEFAULT `move_left`/`move_right` actions, but a netplay match read the per-role split (`move_left_ad` vs `move_left_arrows`, by `multiplayer.is_server()`), so touch movement produced NO input online — a hard blocker for playable mobile MP. FIX: `PlayerController._get_local_input()` now reads `InputCommand.DEFAULT_ACTIONS` in netplay BY DEFAULT (joystick + A/D + arrows all feed it); the per-role A/D-vs-arrows split is now OPT-IN via a `--local-split` command-line flag (read from `OS.get_cmdline_user_args()`), only meaningful for two windows sharing one keyboard. New `tests/run_two_windows.ps1` manual two-window playtest launcher passes `--local-split` (host=A/D, client=arrows); the autopilot `run_net_smoke.ps1` does NOT need it (neither wizard moves). Determinism-safe: netplay input-CAPTURE only — the offline/AI paths are untouched and cross-peer determinism rides the shipped int dicts. VERIFIED: 19 suites BIT-IDENTICAL + LAN smoke byte-identical. **B2a — joystick-direction aiming, the SIM model (commit `c65421b`).** Implements the Creative-Director-decided "move + aim together" model. New `InputCommand.KEY_AIM` (`"a"`) = a signed aim SECTOR in `[-AIM_SECTORS, +AIM_SECTORS]` (AIM_SECTORS=12), folded into `is_empty`/`equals`; the aim drives a projectile's `vx` (a SIM velocity) so it rides the synced input as an INT, never a float. A static `InputCommand.touch_aim_sector` the joystick writes; `capture_local()` injects it. `move_joystick_hud.gd`: the lateral push (`offset.x`) → quantized aim sector (the SAME stick lateral drives movement sign AND aim). `movement_component.gd`: captures KEY_AIM per tick into `_aim_key` (input-derived, replayed → NOT saved) + `get_aim_key()`. `spell_caster_component.gd` + `card_caster_component.gd`: `vx = forward_speed × aim_max_fraction × (sector / AIM_SECTORS)`, PURE FIXED-POINT (no trig → bit-identical cross-platform, the key requirement for desktop-host vs arm64-client play); the keyboard's held-direction duration maps onto the SAME sector scale, so the sim has ONE aim representation and the old hold-to-tilt feel survives at full hold. New `tests/test_aim.gd` asserts the exact fixed-point vx for touch sectors AND keyboard parity (full-hold == touch(±N)). VERIFIED: 19 existing suites BIT-IDENTICAL + LAN smoke byte-identical (KEY_AIM in is_empty/equals doesn't disturb sync) + the new test. **B2b — glowing ground aim arrow, presentation (commit `27b3908`).** New `scripts/visual/aim_arrow.gd` — a flat emissive-cyan UNSHADED arrow mesh on the floor (the arena-border glow look). `spell_caster_component.gd` gains `is_charging()` + `get_aim_sector()` (presentation reads). `core/match_controller.gd` creates the arrow and, each frame in `_process`, points it at the LOCAL wizard's CURRENT aim while it CHARGES a fireball or has an OWNED spell staged on the stack (hidden otherwise). It retargets via `_local_wizard()` (authority-based) EVERY frame, so it follows the CLIENT's own wizard with no hardwiring — avoids the recurring client-perspective bug class BY CONSTRUCTION. The down-court sign reads from the FOE's WizardRig Z (which already encodes the client's `view_flip_z` mirror); the lateral offset is world +X (sim vx never flips) → the arrow matches the ball's visual travel on BOTH peers. New `tests/screenshot_aim_arrow.gd` probe. VERIFIED: 20 suites BIT-IDENTICAL (the per-frame arrow path runs in every match-loading suite) + a screenshot confirmed the arrow renders at the feet, glows cyan, and points up-court tilted to the aim. The live client-perspective check is the Creative Director's 2-window playtest. **REMAINING — item C** (the validation gate, needs the CD's environment): stand up local Nakama (Docker `127.0.0.1:7350`, key `defaultkey`), run `tests/run_net_smoke.ps1 -Mode online` for byte-identical sync over Nakama, then a REAL two-device / real-latency playtest, PLUS the on-device APK playtest of A/B/B2 (touch movement, joystick aiming, the arrow). See the `## 🚧 NEXT PHASE` section below.
- **Sprint 22 / Spawn-Rollback — HEALING EMERALD (2026-06-15):** The LAST `_netplay`-disabled piece is gone — the healing-emerald spawner now spawns rollback-correct on a deterministic sim tick, COMPLETING the spawn-rollback roadmap (the WHOLE match — fireball + cards + round flow + emerald — runs online deterministically). On branch `feature/spawn-rollback-cards`; commit `9f1cdb8`. **The CADENCE is now ROLLED-BACK sim state owned by `RoundFlowResolver`:** the LCG `_em_rng`, the `_em_cd` countdown, and the per-match `_em_cnt` cap are saved (keys `erng`/`ecd`/`ecnt`), advanced in its ACTIVE phase, reseeded (mixing `round_number`) on each round / match reset, and the resolver clears live emeralds IN-TICK on a KO + round reset — replacing the old wall-clock `MatchController._physics_process` LCG cadence (which was `_netplay`-disabled). The actual `SyncManager.spawn` is a HOOK into MatchController (`_spawn_emerald_synced`) so the resolver stays SyncManager-free; "one at a time" liveness is the deterministic `"pickups"` group membership. **`EmeraldController` is now rollback-spawnable:** `_network_spawn(data)` rebuilds it from an int/path payload (`px`/`py` + drift seed + Projectiles scan path); `_ready` joins `network_sync` + idles its local driver under a live session; `_despawn()` routes self-frees through `SyncManager.despawn` (the `_grant_heal` queue_frees + `_clear_emerald` + the struck ball all converted); `_grant_heal`'s presentation (burst / heart / ripple / SFX) is `is_in_rollback`-guarded so the heal + despawn stay deterministic. **`MatchController`** drops the wall-clock `_physics_process` cadence + all its emerald LCG/clear methods + fields, keeping only the spawn hook + the claimed->ripple presentation. **`network_manager._smoke_fingerprint()`** folds in the resolver's emerald cadence (`ecd`/`ecnt`) + the `"pickups"` group, so the new save_state keys are byte-identical under forced rollbacks (confirmed: `ecd1541,ecnt0,npick=0` identical on both peers at tick 300; no emerald spawns in-window at the default 25-40s cadence). VERIFIED: all 19 headless suites BIT-IDENTICAL on Godot 4.6.3-stable (two sweeps); `test_emerald` / `test_emerald_cap` green (the per-match cap is now read from the resolver `_em_cnt`); LAN smoke BYTE-IDENTICAL incl. the emerald fold. The emerald `claimed`->ripple + `HeartPopFX` remain pure-visual (now `is_in_rollback`-guarded). With this, the spawn-rollback roadmap is COMPLETE.
- **Sprint 22 / Online Play Again — DETERMINISTIC SYNCED REMATCH (2026-06-15):** Play Again was DEAD in netplay (`request_rematch` was `_netplay`-guarded; Sub-phase 3 deferred the online rematch as a separate feature). Now it works on BOTH peers, rollback-correct. On branch `feature/spawn-rollback-cards`; commit `4579f67`. **The rematch trigger rides the SYNCED INPUT stream** so the reset lands on the SAME tick on both peers (an off-tick / RPC reset would desync): `InputCommand` gains `KEY_REMATCH` (`"r"`), folded into `is_empty`/`equals` so it is never dropped or mis-compared; `PlayerController.latch_rematch()` sets a render-rate latch that `_get_local_input` injects as `KEY_REMATCH` for ONE synced tick (netplay, authority only); the wizard derives a SAVED `_rematch_requested` flag each tick and exposes `wants_rematch()` (saved key `"rmt"`). `RoundFlowResolver` POLLS `wants_rematch()` on BOTH wizards in its OVER phase and calls `reset_match()` IN-TICK when either requests it; `reset_match()` now emits `round_reset(1)` so MatchController runs the presentation reset (hide the result screen, reset stats + emerald budget, pop ROUND 1) — the step that was missing. `MatchController.request_rematch()` latches in netplay / resets directly (via `reset_match` -> `round_reset`) offline; the SPACE shortcut + the PLAY AGAIN button both route through it. `_on_resolver_round_reset` resets match-scoped stats when `round_number == 1` (the fresh-match marker — round 1 at match start is announced directly, never via `round_reset`). Either player can trigger the rematch (the resolver checks both wizards); mutual consent is a possible future refinement. VERIFIED: 19 suites BIT-IDENTICAL (the new `"rmt"` save_state key keeps determinism); LAN smoke byte-identical; `test_play_again` also drives the resolver's OVER-phase rematch poll (offline).
- **Sprint 22 / Spawn-Rollback Sub-phase 3 — DETERMINISTIC ROUND FLOW (2026-06-15):** The LAST netplay desync hole is closed — ROUND FLOW (KO detection, scoring, the death beat, the post-round break, the round reset) is now tick-deterministic and rolls back. On branch `feature/spawn-rollback-cards`; commit `d54f807`. All 19 headless suites are BIT-IDENTICAL on Godot 4.6.3-stable (run as TWO full sequential sweeps + 8× `test_death_sequence`, flake-free), and the LAN smoke (`run_net_smoke.ps1 -Mode lan -Headless`) is BYTE-IDENTICAL on both peers including the new `rf=` round-flow fingerprint. **The fix — a new `RoundFlowResolver` `network_sync` node (`scripts/arena/round_flow_resolver.gd`, class `RoundFlowResolver`)** that MIRRORS `StackResolver` verbatim (self-drives offline via `local_tick_driver_enabled`, joins `network_sync` via `set_netplay()` in netplay; needs NO SyncManager reference → no compile-order risk). It: (1) DETECTS the KO by POLLING HP each tick in `_network_process` — NOT the `knocked_out` signal, which re-fires on a rollback re-sim and would DOUBLE-COUNT the score; the `phase` int is the once-per-round guard (saved/loaded), exactly like `StackResolver`'s `_resolve_ticks==0`. A same-tick mutual KO deterministically credits the Player (opponent-down checked first). (2) HOLDS `phase` (ACTIVE/DEATH_WAIT/POST_ROUND/OVER) + `countdown` + `player_score` + `opponent_score` + `round_number` + `winner` as rolled-back ints (`_save_state`/`_load_state`). (3) PERFORMS the round reset IN-TICK at countdown-zero (reset both wizards via `reset_for_round`, clear projectiles via each child's `_despawn()`, increment round) — landing on the SAME tick on both peers. This in-tick despawn is the REAL fix for the crash the `ae325f4` guards only papered over (a wall-clock `_clear_projectiles()` landing mid-tick drove a detached entity into the null-`get_tree()` crash). (4) Counts `death_seconds × tick_rate` + `break_seconds × tick_rate` ticks for the death beat + break, which span the correct REAL time because sim ticks stay at 60Hz under slow-mo (the same property `StackResolver` relies on — `Engine.time_scale` scales each tick's delta, not the `_physics_process` frequency; CONFIRMED for the 0.8s death beat at `death_time_scale = 0.3`). **`core/match_controller.gd` demoted to PRESENTATION:** the wall-clock death-sequence `SceneTreeTimer`, the `Time.get_ticks_msec()` post-round break, and the off-tick `_begin_round()` / `_clear_projectiles()` / `_on_knocked_out` are GONE; the four `RoundFlowResolver` phase-change signals (`ko_began` / `break_began` / `match_concluded` / `round_reset`) drive the cinematics (slow-mo, dolly zoom, banners, overlays), each handler `is_in_rollback()`-guarded. A `_sync_mirror()` helper keeps `player_score`/`opponent_score`/`round_number`/`match_state` authoritative for the UI read surface — run BOTH per-frame in `_process` AND synchronously inside each signal handler (a `_process`-only mirror is one frame stale to an `await`-then-read listener; see graveyard). `request_rematch()` routes through `RoundFlowResolver.reset_match()` and is netplay-guarded (a synced online rematch is a separate deferred feature). **`core/network_manager.gd`:** `_smoke_fingerprint()` folds in the resolver's phase/scores/round (`rf=(ph..,ps..,os..,rn..)`), proving both peers agree on round-flow state under forced rollbacks. **Tests** updated for the now-ASYNC (poll-based) KO detection: `test_death_sequence` AWAITS `knockout_began` instead of assuming a fixed frame count; `test_play_again` + `tests/screenshot_postgame.gd` dropped the removed `_post_round_deadline_msec` poke (the break elapses on ticks now). With Sub-phase 3 done, ALL mid-match spawns + the round flow are rollback-correct — the WHOLE match runs online. **Deferred:** the smoke proves the resolver's rollback-STATE determinism (`rf=` byte-identical under forced rollbacks) but did NOT drive a live KO in-window (HP only reached 3 by tick 300; a full KO→death→break→reset cycle is ~8.5s, risking the 40s smoke timeout) — a live online KO is the Creative Director's playtest (the KO/scoring/reset LOGIC is separately proven bit-identical by the offline suites). Still carried (unchanged): the Phase-2 card-presentation rollback guards (cosmetic) + barrier CAPTURE-state rollback; the healing-emerald CADENCE stays `_netplay`-disabled (the next spawn-rollback follow-up — the HEALING EMERALD phase). See the roadmap.
- **Sprint 22 / Spawn-Rollback Phase 2 — CARDS + netplay stability (2026-06-15):** Cards now cast online, rollback-correct, end-to-end. On branch `feature/spawn-rollback-cards`; all 19 headless suites pass on **Godot 4.6.3-stable** (single-player BIT-IDENTICAL), the LAN smoke (`-Mode lan`) is BYTE-IDENTICAL with both peers pulsing a CARD under forced variable-depth rollbacks, and the Creative Director playtested + confirmed offline AND online. **Phase 2a — tick-counted Stack (`StackResolver`):** the new `scripts/arena/stack_resolver.gd` (a `network_sync` node in `match_arena.tscn`) OWNS the stack resolution — it holds the resolution countdown (`_resolve_ticks`), the window fraction, and the last-stager (winner) as ALL-INT saved sim state, and resolves on a COUNTED sim tick that lands identically on both peers. `CardCasterComponent` arms it from its own `_stage()` tick (idempotent — shared clock) and records its side; the counter reactive-lock + WOA now read the resolver's all-int `window_fraction_fp` instead of `TheStack`'s wall clock. MatchController dropped the rollback-unsafe `_stack_entries` resolution + off-tick winner award entirely (it only opens the slow-mo presentation window, rollback-guarded, and reacts to `resolver.resolved` for cleanup; KO/round-reset call `cancel()`). `TheStack` is now PRESENTATION-ONLY (wall-clock slow-mo pacing; its auto-`_expire_window` close was removed — the window closes when the resolver resolves). **The window-tuning bug:** the resolver counts RAW sim ticks (which advance at the fixed tick_rate REGARDLESS of slow-mo — `Engine.time_scale` scales each tick's DELTA, not the `_physics_process` frequency), but the window was first computed as `seconds × tick_rate × stack_time_scale` (3.0×60×0.1 = 18 ticks → resolved in ~0.3 s, "spells resolve instantly"); fixed to `seconds × tick_rate` (180 ticks = 3.0 s, matching the on-screen countdown) — correct offline AND online. **Phase 2b — card spawns through `SyncManager.spawn` (single-player bit-identical):** `_resolve_attack` / `_resolve_counter` / `_resolve_defense` + `FireballController.split_on_barrier` build int/path payloads and route through `SyncManager.spawn`, same proven pattern as the base fireball. `FireballController._network_spawn` extended for the card paths (homing target by ABSOLUTE path `tgt`/`hstr`; explicit lifespan in ticks `life`); new `ProjectileMovementComponent.set_lifespan_ticks(int)` (tick-exact, no float round-trip); the split shards re-spawn via `SyncManager.spawn` from the ball's own `scene_file_path` (the old `duplicate()` left the shard UNREGISTERED → next-rollback crash). New `BarrierController._network_spawn` (deploy + `arm_window_of_affect` from the payload, owner by path) + `_despawn` + the `SyncManager.started` `_ready` gating, and it now SAVES `_woa_armed` (an unsaved capture-arm latch was a rollback hole). The counter's WOA→slow-scale lerp was converted to FIXED-POINT (the float lerp was a cross-peer desync risk). **Phase 2c — cards enabled in netplay:** `player_controller._get_local_input` gates the `KEY_CARD` erase on `_netplay_casting` (was an unconditional strip); `NetworkManager.netplay_casting_enabled = true` on connect (both peers run the unified `_on_peer_connected`, so symmetric). **NETPLAY STABILITY (the rollback crash hunt):** (1) `FireballController._save_state`/`_load_state` switched to STABLE EXPLICIT keys (`"movement"` + `"hit"`) and now include the HitDetectionComponent `_has_hit` latch — the latch was UNSAVED, so a rollback past a hit left it stuck true, the re-sim never re-fired the hit, cross-node damage was reverted-but-not-reapplied → health desync → crash (the explicit keys also keep 4 headless suites — test_determinism / collision_layers / spawn_bounds / spawn_offset — asserting on the stable `"movement"` key). (2) Null-guard EVERY tick-path `get_tree()`/`get_parent()` at its OWN site: `HitDetectionComponent._network_process` + `BarrierController._network_process` bail while `not is_inside_tree()`; `FireballController._network_process` skips while detached (despawn retire window); `_arena_bound_fp` null-checks `get_tree()`; `split_on_barrier` bails on a null parent. The recurring `get_tree()`-null crash (last seen at `hit_detection:96`) is the rollback lifecycle driving a DETACHED entity (despawn retire window, or a wall-clock round-reset `_clear_projectiles()` landing mid-tick) — a PARENT controller's guard does NOT protect a child component. (3) `_on_hit` presentation guarded vs rollback; `MatchController._clear_projectiles` routes each child through `_despawn()` (a bare `queue_free()` dangled the spawn record). **PRESENTATION RETARGETS (client perspective, netplay-only):** client projectiles now MIRROR correctly — every `VisualBridgeComponent` self-computes `view_flip_z` in `_ready` from its netplay role (was set only on bridges present at scene LOAD, so mid-match spawns rendered un-mirrored at the far end); per-role MOVE keys (HOST = A/D, CLIENT = LEFT/RIGHT arrows — two local windows, unambiguous; 4 new InputMap actions + `InputCommand.HOST_ACTIONS`/`CLIENT_ACTIONS`, picked by `multiplayer.is_server()`); cast-button charge-ring retargeted to the LOCAL wizard (`CastButtonHUD.set_caster`); `CardHandHUD.set_caster()` rebinds the 5 caster signals so the CLIENT (red Opponent) sees its OWN card cast fly-out + cooldown dim (was hardwired to the blue Player). NEXT: **Sub-phase 3 — deterministic ROUND FLOW** (the last netplay piece; see roadmap). `HeartPopFX` + the emerald `claimed`→ripple stay pure-visual; the healing-emerald CADENCE stays `_netplay`-disabled (a tick-counted spawn is a Phase-3-style follow-up).

- **Sprint 22 / Spawn-Rollback Phase 1 — Base Fireball (2026-06-14):** First slice of making mid-match spawns rollback-correct so casting works online (the deferred Sprint-21 follow-up). On branch `feature/spawn-rollback`. The BASE FIREBALL now spawns through `SyncManager.spawn()`: `SpellCasterComponent._spawn_projectile` builds a deterministic **int/fixed-point payload** (`px,py,vx,vy,b,mask,dmg,shat,size,src`-path) — the stack-winner boost is consumed in the CASTER's tick (it mutates caster sim state), everything else is pure data — and `FireballController` gained `_network_spawn(data)` (does the positioning + arming + launch, **identical in single-player and on every rollback re-spawn**) and `_despawn()` (routes to `SyncManager.despawn` under a live match, `queue_free` otherwise; all four self-free sites converted). In a netplay match the fireball joins the `network_sync` group and idles its local tick driver in `_ready` (SyncManager drives it; SpawnManager re-sorts the group right after add_child). `KEY_CAST` is no longer stripped in netplay — CARD spawns (`KEY_CARD`) still are, because they resolve off the **wall-clock Stack** which isn't tick-counted yet (= Phase 2). Presentation hooks (`spell_cast`, impact burst) suppress during `SyncManager.is_in_rollback()`. VERIFIED: all 19 headless suites pass (single-player determinism BIT-IDENTICAL — the fireball behaves identically through `SyncManager.spawn`), and the two-instance smoke (both peers pulse-cast) shows both fire fireballs that spawn + fly to **BYTE-IDENTICAL fixed-point positions** (nproj + positions match). NEXT: Phase 2 = tick-count the Stack resolution so attack/counter/defense CARD spawns become rollback-correct; Phase 3 = emerald LCG cadence into a network_sync tick loop. `HeartPopFX` stays pure-visual (no rollback). RULE (spawn-rollback): route a mid-match spawn through `SyncManager.spawn(name, parent, scene, data)` from INSIDE a sim tick (`_network_process`); the entity implements `_network_spawn(data)` (pure int-only setup, runs on spawn AND every re-sim), joins `network_sync` + idles its local driver in `_ready` when `SyncManager.started`, and frees via `SyncManager.despawn`. Side-effects that mutate OTHER nodes' sim state (e.g. consume_speed_boost) must be done by the SPAWNER in its own tick, never in `_network_spawn`.

## ✅ SPAWN-ROLLBACK ROADMAP — COMPLETE (whole match runs online)
> Goal: make ALL mid-match spawns + the round flow rollback-correct so the WHOLE match runs online. **DONE.** **Phase 1 (base fireball), Phase 2 (cards: tick-counted Stack + barrier/spark/ice spawns + cards enabled online), Sub-phase 3 (deterministic ROUND FLOW via `RoundFlowResolver`), AND the HEALING EMERALD (deterministic cadence in `RoundFlowResolver` + rollback-spawnable `EmeraldController`, commit `9f1cdb8`) are ALL DONE on the spawn-rollback branch.** The WHOLE match — fireball + cards + round flow + emerald — now runs online deterministically; online Play Again is also synced (commit `4579f67`). Every milestone was verified with the **19 headless suites (stay bit-identical)** + the **LAN smoke** (`tests/run_net_smoke.ps1 -Mode lan`, `-Headless` for CI — both peers pulse a fireball AND a card under forced random rollbacks → assert byte-identical fingerprints, which fold in wizard positions + HEALTH + projectile count + positions + the round-flow `rf=` state + the emerald cadence `ecd`/`ecnt` + the `"pickups"` group). The live online KO, rematch (Play Again), and round transitions are now Creative-Director-PLAYTESTED on LAN (working). The remaining online-multiplayer work is OUTSIDE this roadmap — **MOBILE enablement + real-online validation; see `## 🚧 NEXT PHASE — MOBILE MULTIPLAYER ENABLEMENT` below.**

**THE PATTERN (proven in Phases 1-2 — reuse verbatim):** spawn via `SyncManager.spawn(name, parent, scene, data)` from INSIDE a sim tick (`_network_process`); the entity implements `_network_spawn(data)` (pure int-only setup, runs on spawn AND every rollback re-spawn); in `_ready`, when `SyncManager.started`, it `add_to_group("network_sync")` + `local_tick_driver_enabled = false`; it frees via `_despawn()` (→ `SyncManager.despawn` under a live match, else `queue_free`). Caster-side state mutations (e.g. `consume_speed_boost`) stay in the SPAWNER's tick. Suppress presentation when `SyncManager.is_in_rollback()`. State that decides a one-shot (a hit `_has_hit` latch, a `_woa_armed` capture-arm) MUST be in `_save_state` or a rollback past it leaves it stuck. Null-guard every tick-path `get_tree()`/`get_parent()` at EACH site (SyncManager drives detached entities).

**TECHNIQUE — deterministic LOCAL trigger via the synced input stream (online Play Again, commit `4579f67`):** to make a LOCAL UI action (a button / shortcut) land on the SAME rolled-back tick on both peers, route it THROUGH the synced input stream instead of an off-tick call or an RPC: a render-rate `latch_*()` sets a flag, `_get_local_input` injects a one-tick input KEY for it (netplay, authority only), the wizard derives a SAVED per-tick flag (in `_save_state`) and exposes a `wants_*()` getter, and a `network_sync` node POLLS that getter each tick and acts IN-TICK. (Mirrors the KO-by-HP-poll pattern; reuse for any "press a button → mutate sim state deterministically online" beat.)

**TECHNIQUE — periodic spawn CADENCE as rolled-back sim state (healing emerald, commit `9f1cdb8`):** an LCG-driven spawn cadence (countdown + RNG + per-match cap) lives in a `network_sync` node's saved state (`_save_state`/`_load_state`), advances in its ACTIVE phase, and reseeds (mixing `round_number`) on each round / match reset — so the spawn TICK is deterministic on both peers. To keep the resolver SyncManager-FREE (no compile-order risk), the actual `SyncManager.spawn` is a HOOK call into a presentation node (`MatchController._spawn_emerald_synced`); "one at a time" liveness is a deterministic group-membership check, not a live node ref.

### ✅ PHASE 2 — CARDS (DONE, 2026-06-15) — attack spark bolt / counter ice wave / defense barrier
Tick-counted Stack resolver + card spawns through `SyncManager.spawn` + cards enabled online; cards cast online byte-identical under forced rollbacks. See the Sprint 22 Phase 2 status entry above for the full breakdown (`StackResolver`, the window-tuning fix, `_network_spawn` for spark/ice/barrier, the hit-latch + `_woa_armed` saves, the detached-entity null-guards, and the client-perspective retargets). **Deferred from Phase 2 (carry into Sub-phase 3 / note as debt):**
- **Card presentation rollback guards (COSMETIC):** the card `spell_cast` / `card_cast` / `spell_staged` emits are NOT `is_in_rollback`-guarded (unlike the fireball impact burst) → POSSIBLE duplicate cast VFX/SFX during an online rollback correction. The SIM is deterministic regardless (smoke-proven); add the guards if the windowed playtest shows doubled VFX.
- **Barrier CAPTURE-state rollback (pre-existing debt):** `_captured_ball` / `_owner_body` are LIVE node refs, not in `_save_state`. Barriers ship rollback-correct for deploy / expire / reflect + the new `_woa_armed` save, but a rollback straddling an in-flight CAPTURE is not yet covered.

### ✅ SUB-PHASE 3 — DETERMINISTIC ROUND FLOW (DONE, 2026-06-15, commit `d54f807`) — the last netplay desync hole
A new `RoundFlowResolver` `network_sync` node (`scripts/arena/round_flow_resolver.gd`, mirroring `StackResolver`) now OWNS the round flow on COUNTED sim ticks: it POLLS HP for the KO (not the rollback-re-firing `knocked_out` signal), holds phase/countdown/scores/round/winner as rolled-back ints, and performs the reset (reset both wizards, despawn projectiles via each child's `_despawn()`, increment round) IN-TICK at countdown-zero so both peers land it on the SAME tick. `MatchController` is demoted to PRESENTATION — its wall-clock death/break timers + off-tick `_begin_round()`/`_clear_projectiles()`/`_on_knocked_out` are GONE, replaced by four `is_in_rollback()`-guarded resolver signals driving the cinematics + a `_sync_mirror()` UI read surface; `network_manager._smoke_fingerprint()` folds in the resolver state (`rf=`). The in-tick despawn is the REAL fix for the `ae325f4` mid-tick-`_clear_projectiles()` crash. ALL mid-match spawns + the round flow are now rollback-correct — the WHOLE match runs online. See the Sprint 22 Sub-phase 3 status entry above for the full breakdown. **Deferred from Sub-phase 3 (note as debt):**
- **Live online KO not driven IN-SMOKE:** the smoke proves the resolver's rollback-STATE determinism (`rf=` byte-identical under forced rollbacks) but HP only reached 3 by tick 300 — a full KO→death→break→reset cycle (~8.5s) risks the 40s smoke timeout. The KO/scoring/reset LOGIC is separately proven bit-identical by the offline suites; a live online KO is the Creative Director's playtest.
- **Synced online rematch — DONE (commit `4579f67`):** Play Again now works on both peers, rollback-correct. The rematch trigger rides the synced input stream (`InputCommand.KEY_REMATCH`, `PlayerController.latch_rematch()` → a one-tick latch, the wizard's SAVED `_rematch_requested` / `wants_rematch()` key `"rmt"`); `RoundFlowResolver` polls `wants_rematch()` on both wizards in its OVER phase and calls `reset_match()` IN-TICK, which now emits `round_reset(1)` so MatchController runs the presentation reset. See the Sprint 22 Online Play Again status entry above.

### ✅ HEALING EMERALD (DONE, 2026-06-15, commit `9f1cdb8`) — the LAST spawn-rollback item (formerly "Phase 3")
The emerald CADENCE moved into `RoundFlowResolver` as ROLLED-BACK sim state (the LCG `_em_rng`, the `_em_cd` countdown, the per-match `_em_cnt` cap — saved keys `erng`/`ecd`/`ecnt`), advanced in its ACTIVE phase, reseeded (mixing `round_number`) on each round / match reset, with live emeralds cleared IN-TICK on a KO + round reset. The actual `SyncManager.spawn` is a HOOK into MatchController (`_spawn_emerald_synced`) so the resolver stays SyncManager-FREE; "one at a time" liveness is the deterministic `"pickups"` group membership. **`EmeraldController`** (`scripts/pickups/emerald_controller.gd`) is now rollback-spawnable: `_network_spawn(data)` rebuilds it from an int/path payload (`px`/`py` + drift seed + Projectiles scan path); `_ready` joins `network_sync` + idles its local driver under a live session; `_despawn()` routes self-frees through `SyncManager.despawn` (the `_grant_heal` queue_frees + `_clear_emerald` + the struck ball all converted); `_grant_heal`'s presentation (burst / heart / ripple / SFX) is `is_in_rollback`-guarded. **`MatchController`** dropped the wall-clock `_physics_process` cadence + all its emerald LCG/clear methods + fields, keeping only the spawn hook + the claimed->ripple presentation; `network_manager._smoke_fingerprint()` folds in the resolver's emerald cadence (`ecd`/`ecnt`) + the `"pickups"` group. **VERIFIED:** 19 suites BIT-IDENTICAL (two sweeps); `test_emerald` / `test_emerald_cap` green (the per-match cap is now read from the resolver `_em_cnt`); LAN smoke BYTE-IDENTICAL incl. the emerald fold (`ecd1541,ecnt0,npick=0` identical on both peers at tick 300). The emerald `claimed`->ripple + `HeartPopFX` remain PURE-VISUAL (now `is_in_rollback`-guarded). See the Sprint 22 Healing Emerald status entry above. **With this, the spawn-rollback roadmap is COMPLETE — the WHOLE match runs online.** Still carried as debt (unchanged, NOT part of the spawn-rollback roadmap): the card-presentation rollback guards (cosmetic) + barrier CAPTURE-state rollback (`_captured_ball`/`_owner_body` not saved).

### WATCH FOR REGRESSIONS
`test_card_system`, `test_stack_resolution`, `test_reflect_freeze`, `test_emerald`, `test_emerald_cap`, `test_ice_shatter` are the suites most likely to break when the Stack/emerald cadence changes — they must stay green + bit-identical.

## 🚧 NEXT PHASE — MOBILE MULTIPLAYER ENABLEMENT (A + B + B2 DONE — only C remains)
> Desktop netplay is DONE + verified (19 suites BIT-IDENTICAL, LAN smoke BYTE-IDENTICAL, Creative Director playtested offline + LAN online incl. rounds / KO / rematch / per-player camera + rumble). To bring multiplayer to **MOBILE** (iOS/Android portrait 9:16), THREE things were needed. **A (cross-compile, commit `151427d`), B (touch-movement input fix, commit `1d5973b`), and B2 (joystick aiming + arrow, commits `c65421b` + `27b3908`) are ✅ DONE** on branch `feature/spawn-rollback-cards` — verified by the 19/20 headless suites (bit-identical) + the LAN smoke (byte-identical); the on-device APK + 2-window aim/perspective checks are the Creative Director's playtest. **Item C — the real-online Nakama + real-latency validation gate — is now the SOLE remaining task** (needs the CD's Docker/devices environment). See the new "Mobile Multiplayer Enablement — A + B + B2" status entry above for the full per-commit breakdown.

### ✅ A — Cross-compile `delta_rollback` for Android arm64 (the ENABLER) — DONE (commit `151427d`)
**SHIPPED:** `libdelta_rollback` cross-compiled for android arm64-v8a (debug 723520 B + release 664720 B, verified AArch64 ELF) via godot-cpp branch 4.2 + NDK r25c; dropped into `addons/delta_rollback/bin/` and the `android.debug.arm64` / `android.release.arm64` lines RESTORED in the `.gdextension`. Also fixed a latent Sprint-21 export bug (`export_presets.cfg` `exclude_filter` was excluding the now-hard-dep `addons/delta_rollback/*` + `addons/godot_state_charts/*` → APK would have crashed on launch), installed the matching 4.6.3 export templates, and re-exported a signed `SpellBop.apk` (~50 MB) whose `lib/arm64-v8a/` carries both native `.so` + the script assets with no desktop/godot-cpp bloat. The on-device extension LOAD is the CD's playtest. (Detail + the NDK-pin junction workaround in the status entry above.) Original task notes preserved below for reference:
The netcode GDExtension `delta_rollback` has NO arm64 build — the `android.*` lines were TRIMMED from its `.gdextension` (desktop-only: Windows/Linux/macOS). `sg-physics-2d` IS already cross-compiled for android arm64 (debug+release, in `addons/sg-physics-2d/lib/`); `delta_rollback` is the ONLY thing blocking the APK from running netcode on a device.
- **First inspect the addon's build system** (`addons/delta_rollback/` — Rust `Cargo.toml`? C++ `SConstruct`/CMake? a prebuilt-only drop?). That dictates everything. If it ships source + a build script, cross-compile its native lib for `arm64-v8a` with the Android NDK (toolchain at `D:\Games\GODOT\android-tools\`). If it's prebuilt-only with no arm64 artifact and no source, that is a HARD blocker to surface immediately (may need the upstream Bimdav project).
- Drop the built `.so` into the addon, RE-ADD the `android.*` entries to `addons/delta_rollback/*.gdextension` (keep the existing desktop entries — do NOT break desktop), then re-export the APK (Sprint 17 export preset + debug keystore live at `D:\Games\GODOT\Wizard Dodgeball`).
- **Verify:** the APK installs + runs on a device AND the rollback extension LOADS on arm64 (failure mode if missing/wrong-arch: "GDExtension library not found" / the autoloads fail). No determinism risk to the desktop sim — this only ADDS a build target.

### ✅ B — Netplay MOVEMENT input for touch / separate devices (the BLOCKER) — DONE (commit `1d5973b`)
**SHIPPED:** `PlayerController._get_local_input()` now reads `InputCommand.DEFAULT_ACTIONS` in netplay BY DEFAULT (touch joystick + A/D + arrows all feed it), fixing the dead touch movement online; the per-role A/D-vs-arrows split is now OPT-IN via a `--local-split` cmdline flag (from `OS.get_cmdline_user_args()`) for the two-window-on-one-keyboard dev harness only. New `tests/run_two_windows.ps1` passes `--local-split` (host=A/D, client=arrows); the autopilot `run_net_smoke.ps1` is unaffected (neither wizard moves). Netplay input-CAPTURE only → determinism-safe (19 suites bit-identical + LAN smoke byte-identical). Original task notes preserved below for reference:
**THE BUG (confirmed this session):** the per-role keyboard scheme — `InputCommand.HOST_ACTIONS` (`move_left_ad` = A) vs `CLIENT_ACTIONS` (`move_left_arrows` = arrows), picked by `multiplayer.is_server()` in `PlayerController._get_local_input()` — is a TWO-LOCAL-WINDOWS hack (one keyboard, two windows, unambiguous). The TOUCH joystick (`scripts/ui/move_joystick_hud.gd`) presses the DEFAULT `move_left`/`move_right` actions (bound to A/D AND arrows). Netplay does NOT read those in a netplay match — it reads the per-role actions — so **touch movement produces NO input online**. (Cast + cards are fine: they use the default `cast_spell` / `card_slot_*` actions, which netplay DOES read — only MOVEMENT is split.)
**THE FIX:** on SEPARATE devices (the real online/mobile case — one peer per device), each peer must read the DEFAULT `move_left`/`move_right` (which the touch joystick + A/D + arrows all feed). The per-role split is ONLY meaningful for two windows on ONE machine. So make `PlayerController._get_local_input()` use `InputCommand.DEFAULT_ACTIONS` in netplay by DEFAULT, and gate the A/D-vs-arrows split behind an OPT-IN for local two-window dev testing (a launch arg, e.g. `--local-split`, or auto-detect both peers on `127.0.0.1`). Result: touch movement works online on mobile; desktop separate-machine play uses natural A/D-or-arrows; only the two-window dev harness opts into the split.
**Keep working:** the LAN smoke (`run_net_smoke.ps1`) launches two local windows but drives CAST by tick and "neither moves" — so it is UNAFFECTED by the move-scheme change. BUT the Creative Director's manual two-window playtest RELIES on the split (A/D vs arrows) to drive both wizards — so the opt-in flag must be passed there (add it to a launch helper + document it), else two local windows both move on A/D **and** arrows again.

### ✅ B2 — Joystick-direction AIMING overhaul + aim-indicator arrow — DONE (commits `c65421b` + `27b3908`)
**SHIPPED (the Creative-Director "move + aim together" model):** B2a (SIM, `c65421b`) — `InputCommand.KEY_AIM` (`"a"`) carries a signed aim SECTOR `[-12,+12]` folded into `is_empty`/`equals`; the joystick lateral push quantizes into it (`move_joystick_hud.gd` via `InputCommand.touch_aim_sector`), `movement_component` captures it per tick (`get_aim_key()`, input-derived → NOT saved), and both casters compute `vx = forward_speed × aim_max_fraction × (sector / AIM_SECTORS)` in PURE FIXED-POINT (no trig → bit-identical desktop-host vs arm64-client), with the keyboard's held-direction duration mapped onto the SAME sector scale (the old hold-to-tilt feel survives at full hold). New `tests/test_aim.gd` pins the exact fixed-point vx for touch sectors AND keyboard parity. B2b (presentation, `27b3908`) — new `scripts/visual/aim_arrow.gd`, a flat emissive-cyan UNSHADED floor arrow that `match_controller.gd` points at the LOCAL wizard's CURRENT aim each frame while it CHARGES or has an OWNED staged spell (hidden otherwise), retargeting via authority-based `_local_wizard()` EVERY frame so it follows the client's own wizard BY CONSTRUCTION (down-court sign from the foe's mirrored WizardRig Z, lateral offset world +X → matches the ball's travel on both peers); new `tests/screenshot_aim_arrow.gd` probe. VERIFIED: 19/20 suites bit-identical + LAN smoke byte-identical + a screenshot. The live client-perspective check is the CD's 2-window playtest. Original task notes preserved below for reference:
An ENHANCEMENT, not a blocker (the current discrete aim still works on touch once B routes the joystick into netplay input), but "now is the time" per the Creative Director, and it should be VALIDATED by C — so sequence it **B → B2 → C** (A in parallel). Two parts; **B2a is a SIM change (determinism-sensitive), B2b is presentation.**

**B2a — Firing angle reads the virtual-joystick DIRECTION on release (overhauls the aim model).** CURRENT model (`SpellCasterComponent._aim_vx_fp` + the same in `CardCasterComponent`; `MovementComponent.get_aim_dir()`/`get_aim_ticks()`): `vx = aim_dir(-1/0/+1 from KEY_X) × (forward_speed × aim_max_fraction[0.5]) × (held_ticks / aim_full_hold_ticks[24])` — i.e. the held LEFT/RIGHT direction × HOLD DURATION (digital, 1-axis). NEW model: the projectile's firing angle = the virtual joystick's 2D push DIRECTION at release (its forward/Y axis too → a full up-court aim CONE, not just left/right tilt). The export comment already anticipates this ("Mobile maps joystick distance here").
- **DETERMINISM (the crux):** the aim sets `vx` = SIM velocity, so it MUST ride the synced input as FIXED-POINT/INT (`InputCommand` is int-only; KEY_X is -1/0/+1). Add a key `KEY_AIM` = the joystick direction QUANTIZED to a fixed sector count (e.g. 16-32 sectors → one int, OR a fixed-point angle); fold it into `InputCommand.is_empty`/`equals`. `move_joystick_hud` exposes its current direction → `capture_local` quantizes it into KEY_AIM; the casters read KEY_AIM on fire → compute vx/vy from the quantized angle (fixed-point). KEYBOARD fallback: map held left/right (± a forward/up key) to discrete sectors, OR keep the current accumulated model for keyboard. RE-VERIFY: 19 suites BIT-IDENTICAL + LAN smoke, and UPDATE the aim tests (`test_spawn_offset` cast-direction-invariant checks + any aim assertion) to the new model.
- **DECIDED (Creative Director): MOVE + AIM TOGETHER, no mode switch.** The joystick's X SIGN drives MOVEMENT (left/right — the existing `KEY_X`, UNCHANGED; movement only ever needs that one axis), and the joystick's FULL DIRECTION/ANGLE drives AIMING (the new `KEY_AIM`) — the angular dimension keyboard lacked is now "free" for the firing angle. Push UP = aim straight down-court; push up-left / up-right = aim down-court at that angle (and the X sign ALSO moves you that way, simultaneously). On fire, the projectile launches at the CURRENT aim angle, so `vx` is driven by `KEY_AIM` (the angle), NOT by hold-duration (the old accumulated tilt goes away). KEYBOARD keeps a discrete fallback (left/right → a few aim sectors, or retain the current held-duration model for keys only).

**B2b — Aim-indicator arrow (PRESENTATION — no determinism risk).** A small glowing-BLUE arrow on the GROUND near the player's feet, pointing at the EXACT firing angle (reads the SAME KEY_AIM / computed angle the caster will use → WYSIWYG). Use the arena-border glow look (emissive cyan, `SHADING_MODE_UNSHADED` — see `MatchController._build_arena_borders`). Shows ONLY when **(A)** the LOCAL player is CHARGING a fireball (live aim, updates with the stick), OR **(B)** a projectile they OWN is on the stack and within 3 s (≤ 180 ticks) of release — read `StackResolver`'s countdown / `window_fraction_fp`; this arrow shows the staged spell's LOCKED aim (captured at stage time). Hidden otherwise. Per-player + client-perspective: it must read the LOCAL wizard's aim (mind the `_player`-hardwired retarget rule — see the client charge-rumble entry / the recurring-class RULE above).

### C — Real online (Nakama) + real-latency rollback (the VALIDATION GATE) — before shipping mobile MP
Everything so far is LAN-tested: localhost, ZERO latency — the smoke FORCES rollbacks via `debug_random_rollback_ticks` to exercise the re-sim path. Mobile MP runs over Nakama (online) with REAL RTT → real, latency-driven rollbacks.
- Stand up the local Nakama (Docker: `127.0.0.1:7350`, key `defaultkey`) and run `tests/run_net_smoke.ps1 -Mode online` (online-host / online-client) → assert byte-identical fingerprints over the Nakama transport (the smoke already supports this mode).
- Then a REAL-latency playtest: two devices / two machines over Nakama — confirm the handshake completes over a real network, the rollback holds under real RTT, and the full match (movement + cast + cards + KO + rematch) stays in sync. THIS is the gate before declaring mobile MP shippable.

### Carried debt (NOT blockers — address opportunistically)
- **Card-presentation rollback guards (cosmetic):** the card `spell_cast`/`card_cast`/`spell_staged` emits are not `is_in_rollback`-guarded → possible duplicate cast VFX/SFX during an online rollback correction. The SIM is deterministic regardless. Add the guards if a windowed playtest shows doubled VFX.
- **Barrier CAPTURE-state rollback:** `BarrierController._captured_ball`/`_owner_body` are live node refs, not in `_save_state` — a rollback straddling an in-flight ball capture is uncovered. Barriers are otherwise rollback-correct (deploy/expire/reflect/`_woa_armed`).

## 🐞 BUG GRAVEYARD (RESOLVED — DO NOT REINTRODUCE)
- **Frame-0 backwards fireball (Sprint 2 hotfix, 2026-06-10):** Godot editor fails to persist SG Physics 2D collision layer/mask UI assignments — all bodies silently fell back to layer 1/mask 1, so spawned fireballs collided with their caster and bounce()d backwards on tick 0. FIX: layers/masks hardcoded in `_ready()` via `scripts/physics_layers.gd` constants (Players=1, Projectiles=2, Walls=4). RULE: never assign SG collision layers through the Inspector. Regression test: `tests/test_collision_layers.gd`.
- **Dead spawn_offset_y export (Sprint 2 hotfix, 2026-06-10):** float @exports cached to fixed-point once in `_ready()` ignore runtime Inspector edits — symptom: "changing the export does nothing". FIX: `spawn_offset_y` now uses a setter that re-caches on write. RULE: any float @export whose fixed-point cache should respond to live tuning needs a re-caching setter. Also note: `scenes/test_area.tscn` overrides `spawn_offset_y=24.0` on its Player instance, shadowing `player.tscn` (10.0) and the script default (48.0) — per-instance scene overrides shadow tuning edits made elsewhere. Regression test: `tests/test_spawn_offset.gd`.
- **Origin spawn trap — local vs global fixed coordinates (Sprint 2 hotfix 2, 2026-06-10):** SpellCasterComponent read the caster's LOCAL `fixed_position` and wrote it into the projectile container's coordinate space — with the player positioned via a parent/wrapper or a container at a different origin, fireballs spawned at midfield (0,0). FIX: spawn path is global end-to-end via `get_global_fixed_position()` / `set_global_fixed_position()` (SG API has getter/setter METHODS, no property). RULE: never mix SG local fixed_position across different parents — cross-node placement must use the global fixed API. Regression test: `tests/test_spawn_offset.gd` TEST 4.
- **Frame-0 facing default (+Y) (Sprint 2 hotfix 2, 2026-06-10):** `cast_direction_y` defaulted to +1, but down-court toward the opposing wall is sim −Y — an unconfigured caster fired at its own baseline on frame 0. FIX: default is now −1 (player wakes facing North/down-court); P2 overrides to +1. RULE: directional defaults must point down-court (−Y).
- **"Stuck at Y=0" — editor float transform never reaches the sim (Sprint 2 hotfix 3, 2026-06-11):** SG Physics 2D keeps the float Node2D transform and the fixed-point transform SEPARATE — dragging a node with the editor gizmo or setting float position/rotation writes ONLY the float side; the deterministic sim reads ONLY fixed_position/fixed_rotation. The player "positioned at 12 m" via the gizmo was at fixed (0,0) the whole time. No origin-teleport code ever existed (three audits + headless probe). FIX: none needed in code. RULE: position/rotate SG nodes ONLY via the fixed_position_x/y / fixed_rotation Inspector fields (raw 64.16: sim units × 65536) — never the 2D viewport gizmo.
- **Physics rotation lock (Sprint 2 hotfix 3, 2026-06-11):** mandated guard — PlayerController._ready() hardcodes fixed_rotation = 0. Character facing is visual-only on the 3D rig. RULE: never rotate/mirror/negative-scale the SGCharacterBody2D or any SG ancestor for facing. Regression test: tests/test_spawn_offset.gd ROTATION INVARIANT checks (cast direction proven invariant to a 180° body rotation and to movement history).
- **Facing-yaw orbit teleport (Sprint 2 hotfix 3, 2026-06-11):** the new rig facing flip (visual_root.rotation.y = 0/PI on A/D) visually teleported the character across the arena because the rig content was not centered on the yawed node's origin — a 180° yaw ORBITS offset content, it does not flip it in place. FIX: face_movement_direction now defaults to FALSE and the yaw is applied to a dedicated @export facing_pivot (falls back to visual_root). RULE: before enabling facing, center the WizardRig on facing_pivot's origin and verify by yawing 180° in the editor — the wizard must spin in place, not move.
- **Sim/visual split-brain — sprites placed instead of bodies (Sprint 2 hotfix 4, 2026-06-11):** the wizard Sprite3D was hand-positioned at z=+10.9m INSIDE the rig while the sim body sat at fixed y=+12 sim units (0.12m, arena center); the wall visual drifted to z=-55m while its collider sat at y=-60 units near center. Result: fireballs spawned (correctly!) at the BODY — which was at the arena center, ~11m behind the art — and instantly hit the wall collider 3 ticks away ("instant bounce-back from center"), while side casts missed the 600-unit-wide collider entirely. FIX: sprites zeroed inside their rigs; sim bodies repositioned to where the art stands (player fixed y=+1100 units -> rig z=+11m; wall fixed y=-1000 -> z=-10m); colliders resized to the measured art (fireball radius 50 units = 1m orb; wall 1152x60 units = 11.5m sprite at pixel_size 0.003; player footprint 40x24 units); arena_half_width 500 to match the 10m floor. RULE: the Node3D rig must sit at the BODY's mapped position (sim x 0.01) with art centered on the rig origin — never hand-place sprites to "fix" a misplaced body.
- **One-frame center flash on projectile spawn (Sprint 2 hotfix 4, 2026-06-11):** a spawned projectile's visual rendered one frame at the scene-default origin because the first state_updated only fired on the next physics tick. FIX: ProjectileMovementComponent.launch() now emits state_updated immediately with the body's spawn position. RULE: any sim entity teleported at spawn must emit its position signal before the next render.
- **Invisible 3D world — .tscn Transform3D is ROW-major (Sprint 3 hotfix, 2026-06-11):** the hand-written match_arena camera basis was authored as basis COLUMN vectors, but .tscn `Transform3D(...)` serializes basis ROWS — the transposed matrix pitched the camera 33 degrees UP at the empty sky, rendering only the default clear color. Proven by an automated screenshot probe (tests/screenshot_probe.gd renders the scene and saves a PNG). FIX: rebuilt camera with an IDENTITY basis (Punch-Out perspective, no rotation). RULE: never hand-author rotated Transform3D values in .tscn — use identity bases positioned by origin, or set rotation from script; ALWAYS verify scene composition with the screenshot probe before handing off.
- **Freed-instance == null re-capture (Sprint 3 tuning, 2026-06-11):** in Godot 4 a FREED object compares equal to null, so a test that guarded "capture once" with `if ref == null:` silently re-captured a fresh instance every time the old one was freed — masking a working lifespan feature as a test failure. FIX: capture-once guards use a dedicated bool flag, never a null check. RULE: never use `== null` to mean "not yet assigned" on refs that may be freed; pair is_instance_valid() with an explicit captured flag.
- **Idle-caster speed-scale erasure (Card sprint, 2026-06-11):** with TWO casters pushing movement penalties through a single-writer set_speed_scale, the idle caster's "no penalty = ONE" write erased the active caster's penalty every tick (last writer wins in child order). FIX: MovementComponent.apply_speed_penalty() MIN-composes into an accumulator that self-resets each tick — active casters re-push every tick, idle casters push nothing. RULE: shared per-tick modifiers must compose (min/sum), never overwrite.
- **Shared sub-resource mutation (Card sprint, 2026-06-11):** instanced scenes SHARE their sub-resources — mutating a spawned barrier/fireball's collision shape (extents/radius) would silently resize EVERY live instance of that scene. FIX: BarrierController.deploy() and FireballController.apply_size() duplicate() the shape before writing. RULE: never mutate a scene sub-resource on an instance without duplicating it first.
- **Vector2 value-type spring helper (Sprint 6, 2026-06-12):** a shared spring function taking (pos, vel) Vector2 parameters silently mutated COPIES — Vector2/float are value types in GDScript parameters. Symptom: UI springs do nothing. FIX: springs are inlined against member variables at every call site. RULE: never write helpers that "mutate" value-type parameters — return values or operate on members.
- **Anchor presets before parenting (Sprint 6, 2026-06-12):** calling set_anchors_preset() on a Control BEFORE add_child(), then setting position/size, recomputed offsets on tree entry and shoved a full-width banner off-center (caught by the screenshot probe). FIX: UI built in code uses explicit position/size with default (0) anchors — canvas space is a stable 1080x1920 under the canvas_items stretch. RULE: in code-built UI, skip anchor presets or apply them only after the node is in the tree.
- **Sim-tick stage countdown vs wall-clock window desync (Sprint 7, 2026-06-12):** the staged spell's release was counted in SIM ticks while the on-screen countdown ran on the wall-clock Stack window — two clocks for one event drifted (playtest: "the spell releases as soon as the card appears"). FIX: the window closing IS the release — MatchController resolves the stack LIFO from TheStack.stack_closed; the sim-tick countdown was deleted. RULE: one event, one clock — never pace the same gameplay beat with both sim ticks and wall time.
- **Container tick-order capture miss (Sprint 11, 2026-06-12):** sibling entities in the Projectiles container tick in CHILD ORDER, so whether the barrier scans before or after a ball moves depends on spawn order — a fast ball could bounce and travel a full tick-step out of the barrier's capture band between scans ("the WOA anticipation sometimes doesn't fire"). FIX: proximity bands are SPEED-AWARE (widened by the target's per-tick velocity). RULE: any per-tick proximity scan against entities that move a comparable step per tick must widen its window by that step — never assume scan-before-move ordering between container siblings.
- **Slow-mo tap loss (Sprint 12, 2026-06-12):** inside the 10% Stack window the sim samples input at ~6 Hz — a quick Shift/card tap fell entirely between two ticks and vanished ("the dash waits for the slow-mo to finish"). FIX: PlayerController polls press edges at RENDER rate and latches them into the next tick's input dict. RULE: edge-triggered actions must be collected at render rate and latched for the sim — never sampled only at tick rate.
- **Bare autoload identifiers in standalone compilation (Sprint 12, 2026-06-12):** scripts referencing the SoundFX autoload by name compiled in-game but FAILED --check-only/standalone loads ("Identifier not found") — the same console-noise class as the earlier editor spam. FIX: a class_name static gateway (Sfx.play) resolves the autoload defensively at call time. RULE: cross-cutting autoloads referenced from many scripts get a class_name static gateway; direct autoload identifiers only inside scenes that guarantee the autoload exists.
- **Freed player in a typed assignment — SFX crossfade crash (Sprint 13 hotfix, 2026-06-12):** SoundFX cached the last AudioStreamPlayer per sound; a naturally FINISHED player frees itself while the cache still holds the dead reference, and pulling that into a TYPED variable (`var prev: AudioStreamPlayer = dict.get(...)`) is a hard script error ("Trying to assign invalid previously freed instance") — crashing on the next retrigger of the same sound. Headless suites never caught it because the dummy audio driver never finishes a stream. FIX: retrieve UNTYPED (Variant), gate on is_instance_valid, only then cast; the finished callback also erases its own cache slot. Repro: tests/manual_sfx_crash_repro.gd. RULE (extends the Sprint 3 freed-instance rule): caches of freeable nodes must be read into UNTYPED variables and validity-checked BEFORE any typed assignment or cast — a typed declaration alone is a crash, not a null.
- **First-draw pipeline compile hitch (Sprint 14, 2026-06-12):** scenes/materials instantiated mid-fight (the stack resolution spawning projectiles, BurstFX building a fresh StandardMaterial3D per burst) compile shaders/render pipelines on FIRST DRAW — a visible lag spike exactly at the slow-mo exit. FIX: MatchController warms every projectile/barrier scene + burst type with an occluded draw at round start; BurstFX caches meshes/materials per look and duplicates compiled templates. RULE: anything that can be instantiated mid-fight must be drawn once during a loading beat; never construct fresh materials inside gameplay-time spawns.
- **Label autowrap property-order trap (Sprint 16, 2026-06-12):** setting a Label's `size` BEFORE enabling `autowrap_mode` snaps the control to its single-line minimum width, so the wrap never engages — descriptions rendered as one clipped line (found only by cropping/upscaling a screenshot). Related: `clip_text` is a single-line ellipsis mode, not rect clipping — wrapped labels need `clip_contents`. RULE: for code-built wrapped labels, set autowrap/clipping/font FIRST, size second, text last; and verify text layout in PIXELS, not in code review. Also logged: a stored-and-killed pattern for transient UI tweens (a stale ROUND-N fade ate the VICTORY title).
- **No Android binary for the native sim extension (Sprint 17, 2026-06-13):** sg-physics-2d — the GDExtension the ENTIRE deterministic sim depends on (SGFixed, SGCharacterBody2D, etc.) — ships ONLY windows/linux x86_64 binaries upstream; there is NO Android build for ANY release version (confirmed via the gitlab releases API). A naive Android export installs fine but HARD-CRASHES the instant a match scene loads, because the arm64 .so is missing. FIX: cross-compiled sg-physics-2d v1.0.0-alpha13 (the exact version the project uses) from gitlab source for android arm64 — Android NDK r25c, the godot-cpp submodule, scons platform=android arch=arm64 for BOTH template_debug and template_release; dropped the two .so files into addons/sg-physics-2d/lib/ and added android.debug.arm64 / android.release.arm64 entries to sg-physics-2d.gdextension. RULE: before exporting to ANY new platform, every GDExtension the project depends on must have a native binary for that platform's arch/target — verify by unzip-listing the exported APK/IPA and confirming the .so is under lib/<abi>/. Same will apply to iOS later (needs an ios arm64 build of sg-physics-2d).
- **delta_rollback was dead weight, not a dependency (Sprint 17, 2026-06-13):** the delta_rollback GDExtension was loaded at startup but its classes (SyncManager etc.) are referenced ONLY in code COMMENTS as a future plan — never instantiated; the game runs on PlayerController's own local tick driver. Its .gdextension even declared android libs whose .so files do not exist on disk, so it would have errored on device. FIX: disabled it by renaming addons/delta_rollback/bin/delta_rollback.gdextension -> .gdextension.disabled (and its .uid). All 6 original suites still pass and determinism stays BIT-IDENTICAL, proving nothing depended on it. RULE: a GDExtension sitting in addons/ loads even if its "plugin" is not enabled; disable unused ones before mobile export so they don't demand per-platform binaries.
- **Projectiles/walls spawned outside the arena (Sprint 17 hotfix, 2026-06-13):** every spawn site (_resolve_attack/_resolve_counter/_resolve_defense in card_caster_component.gd, _spawn_projectile in spell_caster_component.gd, split_on_barrier in fireball_controller.gd) placed the object at caster_pos.x with NO bounds clamp, so a WIDE object (Icey Retort frost wave, ±200u) spawned while the caster sat at the lane edge reached ~150u past the side walls. The aim mechanic was a red herring: _aim_vx_fp tilts launch VELOCITY only, never spawn position — but holding a direction to bank aim parks the caster at the edge, so it correlated. FIX: a deterministic fixed-point spawn-X clamp — MovementComponent.clamp_spawn_x_fp(x, half_extent, bound) (STATIC, pure clampi on 64.16 ints, limit floored at 0) + MovementComponent.arena_half_width_fp() — applied at all five sites, clamping the object's FULL extent (centre ± half-extent) inside the caster's MovementComponent bound (read at spawn time so scene overrides win); aim/velocity untouched so spells stay aimable. Regression: tests/test_spawn_bounds.gd. RULES: (a) clamp every spawned gameplay object's spawn ORIGIN so its full collider extent stays inside the arena bound, using the POST-resize half-extent (apply_size/deploy) and the scene's MovementComponent bound — never a hardcoded constant; (b) aim is VELOCITY ONLY and must NEVER become a spawn-position factor (reinforces the spawn-offset graveyard rule). FORWARD-LOOKING CAVEAT (from the adversarial review): the bound is currently a SHARED global property (both wizards = 400, symmetric court), so the shard reading "the first wizard's mover" and the casters' from_float(400) defensive fallback are correct today. If arena bounds ever become PER-PLAYER asymmetric, revisit split_on_barrier's bound lookup (have the shard carry its caster's bound) and the fallback constants.
- **Wizard sprite sank into the floor — animator zeroed the sprite offset (Sprint 18 follow-up, 2026-06-14):** the charge-shake in wizard_animator_component.gd wrote `_sprite.position = Vector3(jitter)` while charging and reset it to `Vector3.ZERO` otherwise, EVERY frame — overwriting the Sprite3D's authored local offset (0, 1.13, 0) and dropping the sprite centre to y=0, so its lower half rendered below the floor plane. FIX: capture `_sprite_base_pos = _sprite.position` in _ready() and offset jitter from that base (reset TO the base, never to zero). Compounding cause: the sprite used full billboard (mode 1), which tilts ~23deg toward the down-angled camera and dips the bottom into the floor — switched to Y-axis billboard (mode 2, stays upright). RULE: a transient visual effect (shake/jitter/recoil) must offset from a CAPTURED base transform, never assign an absolute value or reset to zero — that silently destroys authored offsets. For billboard characters on a floor plane, use Y-billboard so they stay upright and their base stays on the ground.
- **Two headless Godot instances deadlock on the import cache (Sprint 19, 2026-06-14):** running two `godot --headless` processes against the SAME project at once HANGS both (0 output, never exits) — they contend on the `.godot/` import cache/lock. Symptom: a test that ran fine alone "hangs" when another instance is live. FIX/RULE: run the headless test suites STRICTLY SEQUENTIALLY (one finishes before the next starts); never fan them out in parallel.
- **Stale pickup reference after a spawner refresh (Sprint 19, 2026-06-14):** the healing-emerald spawner REFRESHES (frees the un-claimed emerald and spawns a fresh one) every interval, so a test holding a captured emerald node reference was poking a FREED instance after the refresh (with a fast test cadence). FIX/RULE: in tests, freeze the spawner after capture (set MatchController.emerald_scene = null) or re-fetch the live emerald from the "pickups" group each time — never hold a long-lived reference to a refreshable spawned entity.
- **Staggered slow-mo resolution invisible at deep dilation (Sprint 20, 2026-06-14):** the stack's LIFO one-at-a-time release was mechanically correct (ignore_time_scale wall-clock timers, real gaps) but looked SIMULTANEOUS in play — at the 10% reaction dilation a 0.2 s real gap is only ~0.02 s of SIM time, so consecutive spells barely separated before normal speed resumed. FIX: step the dilation up to a moderate resolve_time_scale (0.3) during the resolution beat AND lengthen the inter-spell gap (0.2 -> 0.4 s) so each release visibly travels. RULE: a staggered PRESENTATION beat playing under time dilation must either run at a time scale where its inter-event gap produces visible on-screen motion, or lengthen the gap to compensate — a real-time gap alone is not enough when the world is in deep slow-mo.
- **GDScript lambda captures locals BY VALUE (Sprint 20 test, 2026-06-14):** a headless test captured a result into a local `Array` and a connected signal lambda did `arr = [true, ...]` to record the event — but GDScript lambdas capture local variables by VALUE, so REASSIGNING the captured variable inside the lambda never escaped, and the outer array stayed unchanged (the assertion failed even though the signal fired). FIX: MUTATE the captured array's ELEMENTS (`arr[0] = true`) — element mutation acts on the shared array object — or record into a MEMBER variable (members aren't value-captured). RULE (extends the value-type-parameter graveyard rule): never REASSIGN a captured local inside a GDScript lambda expecting it to escape; mutate elements/members instead.
- **Stack resolved staggered but the DISPLAY cleared at once (Sprint 20 round-3, 2026-06-14):** the staggered LIFO projectile releases were correct, but the on-screen MTG-style stack display flew ALL card faces away together on stack_closed — so the thing the player actually watches looked simultaneous, masking a working mechanic through three bug reports. FIX: peel the display cards ONE AT A TIME off each spell's card_cast (in lockstep with the staggered releases); only a KO/forced close drops the whole pile. RULE: when a mechanic is staggered in the SIM, its primary on-screen REPRESENTATION must be driven by the same per-step event (card_cast), not by the one-shot "window closed" signal — verify the thing the PLAYER LOOKS AT resolves one step at a time, not just the underlying entities.
- **Reflected wide projectile veered off on a random ricochet (Sprint 20 round-3, 2026-06-14):** the anti-tunnel band made a straight reflected ice wave connect, but the defense barrier's random lateral ricochet (position-parity sign) sent the court-wide frost wall off-angle, so it intermittently missed the thrower it was supposed to freeze ("sometimes it doesn't freeze"). FIX: a reflected FROST wave flies straight (no ricochet) and gently homes onto its original thrower; fireballs keep the ricochet. RULE: a WIDE area projectile that must reliably return to a specific target should not inherit a randomized ricochet meant for point projectiles — reflect it straight (and/or home it) so its width is an asset, not a way to whiff past.
- **New class_name not picked up by `-s` headless runs until re-import (Sprint 20 round-4, 2026-06-14):** a brand-new script with `class_name HeartPopFX` failed to resolve ("Identifier HeartPopFX not declared in the current scope") when another script referenced it under `godot --headless -s test.gd`, because the global class cache (.godot/global_script_class_cache.cfg) is only rebuilt on an editor scan / `--import`, not on a `-s` script run. FIX: run `godot --headless --import` once after adding a new class_name (it logs `update_scripts_classes | HeartPopFX`), or reference the script via preload instead of its global name. RULE: after adding any new `class_name`, re-import the project before running headless `-s` suites that reference it. (Also: the stale `~lib*.dll` GDExtension shadow copy can make `--import` exit non-zero while still updating the class cache — delete it / it is gitignored.)
- **delta_rollback re-enabled for desktop multiplayer (Sprint 21, 2026-06-14):** the Sprint-17 entry DISABLED delta_rollback (it was unused dead weight with no Android `.so`). Sprint 21 INTENTIONALLY re-enables it as the real LAN/online rollback layer — but ONLY the desktop libraries (Windows/Linux/macOS exist on disk); the `android.*` library lines were TRIMMED from `addons/delta_rollback/bin/delta_rollback.gdextension` so an Android export doesn't demand the missing arm64 `.so` (which would hard-crash on device, per the original entry). RULE: delta_rollback is now a DESKTOP dependency; before any Android multiplayer build, cross-compile its arm64 `.so` (mirror the sg-physics-2d Sprint-17 work) and restore the `android.*` entries. Re-confirmed: stale `~lib*.dll`/`.TMP` GDExtension shadow copies — for **both** delta_rollback AND sg-physics-2d — break `--import` with "dynamic library not found"; delete them (gitignored). One `~...TMP` may be LOCKED by a live Godot process — that one is the active hot-reload copy, not stale; harmless.
- **Godot-3 API survivors in the vendored delta_rollback (Sprint 21, 2026-06-14):** `RPCNetworkAdaptor.is_network_master_for_node()` called `node.is_network_master()` and `SyncManager.get_latest_input_for_node()` called `node.get_network_master()` — BOTH removed in Godot 4.0 (renamed to `is_multiplayer_authority()` / `get_multiplayer_authority()`). They parse fine but CRASH the instant SyncManager drives networked input. FIX: two one-line compat shims (identical semantics). RULE: when re-enabling a Godot-3-era addon, grep it for `is_network_master(`/`get_network_master(`/`rset(`/`yield(`/`.rpc_unreliable(` before trusting the hot path.
- **Autoloads aren't in /root until after frame 1 in `-s` SceneTree runs (Sprint 21, 2026-06-14):** a probe that read `/root/SyncManager` inside `_init()` found it missing — in a custom-SceneTree `-s` script the autoloads are wired in AFTER `_init`, on the first frame. FIX: `await process_frame` before touching any autoload (the screenshot/probe tests already do this). RULE: never read an autoload synchronously in a `-s` test's `_init`. (Process note: the smoke launcher hit two PowerShell-5.1 traps — `$ErrorActionPreference="Stop"` + native stderr (harmless `DPITexture` warnings) wraps into a terminating `NativeCommandError`; and a spaced project path passed in a `Start-Process -ArgumentList` array splits on the space. Fixes: drive success from logs not exit codes, and use `-WorkingDirectory $proj` + `--path .` instead of a spaced path. Also: non-ASCII chars in a `.ps1` (em-dash, …) corrupt under PS 5.1 — keep launcher scripts ASCII.)
- **Client camera-flip into a one-sided arena = unlit void (Sprint 21 fix, 2026-06-14):** to give the netplay CLIENT a "behind my own wizard" view, the first attempt ROTATED the camera 180° to the opponent's baseline. The geometry was correct (verified by on-screen projection — camera behind red, looking up-court at blue) but it rendered BLACK/empty: the arena's starfield backdrop, the three OmniLights, and the fog gradient are all authored ONE-SIDED for the host's +Z viewpoint, so the reverse angle is backstage (no sky, unlit; the unshaded billboard wizards barely read). Patching it (mirrored backdrop quad + ambient boost + front fill light + modulate boost) never made it acceptable. FIX: don't move the camera — MIRROR THE PRESENTATION. `VisualBridgeComponent.view_flip_z` negates the visual **Z only** (X kept, so the client's left/right controls stay correct) so the client's wizard renders at the NEAR, fully-lit baseline and the foe downcourt; the AUTHORED camera (pointed at the client's rig via `camera_rig.set_follow_target`) frames it with ALL the existing lighting/backdrop — identical quality to the host view. RULE: to give a second viewpoint of a ONE-SIDED authored 3D scene, mirror the PRESENTATION into the authored camera's framing — never relocate the camera into the unlit backstage. (Pure presentation; the deterministic sim is identical on both peers. Confirmed this round: the rollback INPUT AUTHORITY was correct ALL ALONG — a host-only-press smoke proved the host drives the blue Player and the Opponent stays put; the "frozen blue Player" report was the client-perspective confusion this mirror resolves. STILL OPEN: the client HUD is host-oriented — the corner health bar shows the foe and the floating bar shows the client's own; swap `MatchHUD.health_path`/`opponent_health_path` on the client next.)
- **SyncManager bare identifier won't COMPILE in early-graph scripts (Sprint 22 Phase 2, 2026-06-15):** the bare `SyncManager` autoload identifier failed to COMPILE ("Identifier not found") inside `scripts/player/components/card_caster_component.gd` — it is pulled into the compile graph EARLY (via `AIBrainComponent`), so it compiles BEFORE the `SyncManager` autoload registers as a GDScript global. `--import` does NOT fix it (the compile order, not the class cache, is the problem). FIX: resolve the autoload by `/root` NODE PATH (`card_caster` has a `_sync_manager()` helper). `spell_caster_component` / `fireball_controller` compile LATE and use the bare `SyncManager` identifier fine. RULE (extends the Sprint-12 `Sfx.play` gateway rule to a third failure mode — COMPILE not load): a script that may be pulled into the compile graph before an autoload registers must reach that autoload via `/root/<Name>`, never the bare global.
- **Tick-path `get_tree()`/`get_parent()` must be null-guarded at EACH site (Sprint 22 Phase 2, 2026-06-15):** the rollback `SyncManager` drives an entity while it is DETACHED from the tree (the ~20-tick despawn retire window; or an off-tick teardown — a wall-clock round-reset `_clear_projectiles()` — landing mid-tick), so any `_network_process` reading `get_tree()`/`get_parent()`/`get_nodes_in_group()` hits NULL and crashes. This RECURRED: `FireballController` was guarded, then its `HitDetectionComponent` CHILD crashed at `hit_detection:96` — a PARENT controller's `is_inside_tree()` guard does NOT protect a child component. FIX: every tick-path tree/parent access bails on `not is_inside_tree()` (or null-checks the result) at its OWN site. RULE: in rollback netcode, guard the tree at the exact site that reads it — never assume a parent guard, a sibling guard, or "it's in the tree because it's ticking" covers a child. (The real determinism fix for the off-tick clear is Sub-phase 3 — deterministic round flow; these guards only stop the CRASH.)
- **Rollback `_save_state` keys must be STABLE (explicit), not node-name-derived (Sprint 22 Phase 2, 2026-06-15):** `FireballController` aggregated its components' save dicts by CHILD NODE NAME; switching to explicit keys (`"movement"`, `"hit"`) was needed both to add the hit-latch and to keep determinism. But it ALSO broke 4 headless suites (test_determinism / test_collision_layers / test_spawn_bounds / test_spawn_offset) that read the saved fireball state by the literal key `["movement"]` — a node rename silently changed the key and the assertions missed. FIX: explicit, stable string keys for every saved component. RULE: rollback `_save_state` keys are a CONTRACT (tests + the cross-peer state hash both depend on them) — derive them from explicit constants, never from a node name that a refactor can change. (Companion: state that decides a ONE-SHOT — a `_has_hit` damage latch, a `_woa_armed` capture-arm — MUST be in `_save_state`, or a rollback past the event leaves it stuck and the re-sim never re-fires it → cross-node desync.)
- **Slow-mo scales tick DELTA, not tick FREQUENCY — "the stack resolves instantly" (Sprint 22 Phase 2, 2026-06-15):** `Engine.time_scale` (the 10% Stack slow-mo) scales the DELTA passed to each `_physics_process` tick — it does NOT change how OFTEN `_physics_process` fires; the sim still ticks at the fixed `tick_rate`. So a TICK COUNTDOWN is time-scale-INDEPENDENT and must NOT be multiplied by the slow-mo factor. The `StackResolver` window was first computed as `seconds × tick_rate × stack_time_scale` (3.0×60×0.1 = 18 ticks) and resolved in ~0.3 s while the wall-clock countdown still read ~2.7 s ("spells resolve instantly"). FIX: `window = seconds × tick_rate` (180 ticks = 3.0 s). RULE: a sim-tick countdown counts REAL ticks at the fixed rate regardless of `Engine.time_scale` — never fold the slow-mo factor into a tick count (that factor belongs only to the wall-clock PRESENTATION timer it paces).
- **`SceneTree.process_frame` fires BEFORE `_process` — a `_process`-only UI mirror is one frame STALE (Sprint 22 Sub-phase 3, 2026-06-15):** `MatchController` mirrors the `RoundFlowResolver`'s rolled-back ints (`player_score`/`opponent_score`/`round_number`/`match_state`) into a plain read surface for the UI. A mirror synced ONLY in `_process` is one frame BEHIND any listener that `await`s a resolver signal and IMMEDIATELY reads the mirrored field — because `process_frame` is emitted before `_process` runs that frame. Symptom: `test_round_flow` failed ONLY on `match state is MATCH_OVER` while the scores (set directly in the signal handler) passed. FIX: also sync the mirror SYNCHRONOUSLY inside each signal handler (`_sync_mirror()` is called in BOTH `_process` AND every phase-change handler). RULE: when a signal handler must leave a derived/mirrored field readable by an `await`-then-read listener, update that field INSIDE the handler — do not rely on a per-frame `_process` mirror, which a `process_frame`-ordered listener reads one frame stale.
- **Poll-based detection is ASYNC (next sim tick) — a fixed-frame test wait flakes (Sprint 22 Sub-phase 3, 2026-06-15):** `RoundFlowResolver` detects the KO by POLLING HP in `_network_process` (a sim tick), where the old `knocked_out` signal was SYNCHRONOUS. A test that KO'd via `apply_damage()` then asserted after a FIXED 2 `process_frame`s flaked ~20%: in headless, idle frames can outrun the 60Hz physics, so 2 idle frames sometimes contain ZERO physics ticks (no poll has run yet, no detection). FIX: `await` the detection (the `knockout_began` signal) with a timeout — never assume a frame count. RULE (extends the Sprint-21 "autoloads aren't in /root until frame 1" timing class): when detection moves from a synchronous signal to a per-TICK poll, every test that observed it must `await` the resulting signal, because an idle/`process_frame` wait is not guaranteed to contain a physics tick.
- **60Hz-under-slow-mo CONFIRMED for the death beat (Sprint 22 Sub-phase 3, 2026-06-15):** the `RoundFlowResolver` counts `death_seconds × tick_rate` ticks for the cinematic death beat (and `break_seconds × tick_rate` for the post-round break). Empirically PROVEN that `death_seconds × tick_rate` ticks span `death_seconds` of REAL time EVEN at `death_time_scale = 0.3` — `test_death_sequence` shows the 0.8s beat lands in ~0.8s (passed all 13 runs: the two sequential sweeps + 8× repeats + the original). This is the SAME property the `StackResolver` window relies on: `Engine.time_scale` scales each tick's DELTA, not the `_physics_process` FREQUENCY, so a tick count measures real time regardless of slow-mo. RULE (the positive corollary of the "slow-mo scales tick DELTA not FREQUENCY" entry above): to time a REAL-duration beat that must survive slow-mo, count `seconds × tick_rate` SIM ticks — never a wall-clock timer (which the dilation would stretch) and never the slow-mo-scaled tick count.

## 🪦 MECHANICS GRAVEYARD (REJECTED — DO NOT CODE OR SUGGEST)
- NO MTG-style land cards or resource cards.
- NO true 3D Z-axis character movement or over-the-shoulder aiming.
- NO complex 3D hitbox detection.
- NO proactive counter spell spamming.
- NO losing a spell's charge progress instantly upon breaking touch contact.
- NO writing custom netcode, custom rollback math, or tracking vectors from scratch.
- NO player DASH ability (the Left-Shift blink was removed wholesale in Sprint 19 by Creative Director decision — do not reintroduce it). NOTE: CardResource.movement_dash_distance is a SEPARATE dormant card-effect data field, not the player dash, and may remain.
